# Meta Business Agent 简易版 —— 需求说明

> 对标产品：Meta Business Agent（2026-06-03 发布）https://about.fb.com/news/2026/06/meta-business-agent/
> 架构参考：《The Architecture of Meta Business Agent on WhatsApp》（Meta Developers，2026-06-04）
> 配置模型参考：Meta Business Agent 完整指南（Standing Tech，2026-07-03）
> 参考实现：https://github.com/entingwu/EconChatbot/
> RAG 实施计划：`RAG_ref.md`（本仓库根目录）。通用选型依据见 `knowledge_base/RAG_技术选型总结.md`
>
> **交付策略**：新建仓库重写，先交付 MVP（14–18 人天），再按档位扩展。各功能的档位标记见每节标题。

---

## 1. 项目背景与目标

### 1.1 项目背景

商家通过消息渠道与消费者沟通。消息是 7×24 小时到达的，中小商家没有专职客服，人工无法覆盖。

Business Agent 与普通聊天机器人的区别在于：**它不是写死的问答脚本，而是一个可配置的 AI 助手**——商家把业务信息、FAQ、政策文档和行为指令配置进去，Agent 据此作答、办事，办不了时交给人。配置质量直接决定 Agent 质量。

------

### 1.2 官方架构的三根支柱

| 支柱 | 含义 | 章节 |
|---|---|---|
| Agent 配置 | Agent 知道什么、怎么回、能做什么 | 3.1 |
| 会话内体验 | 在对话里把事办完，不把人跳走 | 3.2 |
| 业务能力 | 答问、推荐、成交、接管 | 3.3 |

整体要求「接入既有技术栈」——Agent 不自建业务系统，通过接口调用商家现有的商品、订单服务。

------

### 1.3 三档优先级

| 档 | 判据 | 人天 |
|---|---|---:|
| **第一档** | 砍了就不能称为 Business Agent，且演示可见 | 6.5–9 |
| **第二档** | 真实运行需要，演示可后补 | 11.5–12.5 |
| **第三档** | 增强项，可不做 | 10–13 |

加上内核移植（3–4）与前端（4–5）：

| 方案 | 内容 | 合计 | 周期 |
|---|---|---:|---|
| **MVP** | 第一档 | **14–18** | 约 3 周 |
| 完整 | 一 + 二档 | 26–31 | 5.5 周 |
| 全量 | 三档全做 | 36–44 | 8 周 |

------

### 1.4 明确不做

真实渠道接入（用自建 Web 渠道模拟）、灰度与白名单、预约、线索资格认定、支付网关、多租户、计费、多语言、SSE、商家简报、退换货与投诉工单（走人工接管）、数字人。

------

# 2. 用户角色

**消费者**：咨询业务信息与政策、查订单、寻求商品推荐、寻求人工。

**商家 / 客服坐席**：接管 Agent 处理不了的会话。

**运营 / 开发者**：配置知识与技能指令，维护商品目录。

------

# 3. 功能需求

## 3.1 Agent 配置

### 3.1.1 知识源分类 【第一档】

三类知识源，适用场景不同，不可互相替代：

| 类型 | 内容 | 适用于 | 检索方式 |
|---|---|---|---|
| **实时业务数据** | 订单状态、物流进度、商品价格、库存 | 易变数据 | 调业务接口，不缓存 |
| **FAQ** | 高频问答对 | 需要精确可控答案的问题 | 精确 / 关键词匹配 |
| **文档知识** | 政策文档、商品说明、服务介绍 | 长文档，答案分散在段落里 | **RAG 检索** |

**配置纪律（替代路由层）**：易变数据一律不写入知识库。商品价格、库存、订单状态若被写进文档，会造成检索到的旧值与接口的新值并存。守住这条纪律，即可省去优先级路由层的实现（见 3.1.2 附注）。

------

### 3.1.2 RAG 检索 【第一档】

**入库**

- 文件类型：Markdown、TXT、CSV
- 支持知识源的新增、更新、删除，更新后索引同步生效
- 记录知识源的类型、名称、入库时间

**切分**

- 按语义边界切分，优先在标题与段落处断开
- 分片大小与重叠长度可配置
- FAQ 与 CSV 类结构化内容按条目切，不做语义切分

**向量化与索引**

- Embedding 模型可替换，通过配置指定
- 向量库可替换，通过统一接口抽象
- 每个分片保留来源标识：知识源 ID、标题、片段位置

**检索**

- 向量相似度检索，Top-K 可配置
- 设相似度阈值，低于阈值视为未命中
- 命中多个分片时按相似度排序取 Top-K 拼接进上下文

**生成约束**

- 回复必须基于命中分片组织，**不得超出分片信息作答**
- 内部记录命中的分片 ID 与相似度，回复可溯源

**不知道时怎么办**

- 未命中或低于阈值时，走兜底话术，说明需要什么信息，或引导转人工
- **禁止编造**，也不允许用模型自身通用知识替代商家知识

**默认参数**（全部可配置，选型与替换路径见附录 C.4）

| 参数 | 默认值 | 说明 |
|---|---|---|
| 分片大小 | 500–800 tokens | 语义边界优先，宁可偏小不可跨节 |
| 重叠长度 | 80–150 tokens | 约分片长度的 10–20% |
| 检索 Top-K | 5 | 拼接进上下文的分片上限 |
| 相似度阈值 | ~~余弦 0.35~~ → **实测校准为 0.58** | 低于阈值视为未命中，直接走兜底。见下方注 |
| 上下文分片总长 | ≤ 2000 tokens | 超出按相似度从低到高截断 |
| FAQ / CSV 切分 | 一条一片，不做语义切分 | 条目本身即语义单元 |

> 阈值是唯一必须用真实语料校准的参数。拍脑袋定值会同时制造两类事故：该兜底的编造了，该回答的说不知道。

> **实施记录（2026-08-28）：表里的 0.35 是拍出来的，实测站不住。** `text-embedding-v3` 的余弦分数整体偏高——「推荐一部最近好看的电影」这种完全不相干的问题都能拿到 0.31，0.35 几乎不过滤任何东西。用 34 条样本（有答案 22 / 无答案 12）按 C.4.6 的方法校准：首轮区间重叠，**按规范先查切分而非改阈值**，发现 FAQ 的「常见问题：」共同前缀给所有 FAQ 向量注入了公共分量；改成 entry 模式只 embed 正文后，可分区间收敛到 (0.5605, 0.6030)，取中点 **0.58**。上线实测有答案召回 22/22、无答案正确兜底 11/12。
>
> 这条留作教训：**这张表里的其他数字同样是拍的**，只是它们没有阈值那么敏感，所以没暴露。换 Embedding 模型必须重跑 `-m business_agent.knowledge.ingest calibrate`，旧值一律作废。

> 附注：知识源之间的优先级路由（命中即止）在第二档实现。第一档靠 3.1.1 的配置纪律规避冲突。**（截至 2026-08-28 仍未实现，配置纪律守住了，没有出现过知识源冲突。）**

------

### 3.1.3 业务信息 【第二档】

营业时间、地址、联系方式、支付方式、退换货政策、配送说明等稳定事实，以结构化字段配置。第一档期间这些内容暂时写进 FAQ。

------

### 3.1.4 技能指令 【第二档】

针对特定情境的行为指令，三要素：

| 字段 | 说明 |
|---|---|
| `title` | 技能标识 |
| `description` | 何时启用 |
| `skill` | 具体行为指令 |

要求：以配置方式新增不改引擎；指令注入提示词模板不写死；只影响生成式回复的行为与语气，不改变知识来源与工具调用；**不得突破准确性约束**——商家写"任何问题都要给出答案"，不能导致检索未命中时编造。

------

### 3.1.5 工具与业务系统集成 【第一档】

- Agent 不自建业务系统，通过工具调用商家既有服务
- 工具统一注册，新增工具不改对话引擎
- 每个工具声明：名称、入参、出参、是否为写操作
- 写操作声明幂等键（第二档引入写操作时生效）
- 工具调用失败时如实告知，不用其他信息替代

**第一档工具**：查订单、查物流、查商品、检索商品目录
**第二档新增**：创建订单
**第三档新增**：发放优惠券

------

## 3.2 会话内体验 【第一档】

核心原则：在对话里把事办完，不把消费者跳走。

| 结构化消息 | 用途 | 档位 |
|---|---|---|
| 快捷回复按钮 | 收敛提问、推进成交 | 第一档 |
| 商品卡片 / 列表 | 推荐结果展示与多选一 | 第一档 |
| 订单卡片 | 订单信息展示 | 第一档 |
| 订单摘要确认卡片 | 下单前确认 | 第二档 |

- Agent 可发出业务对象消息；消费者点击后该对象回传并自动填充槽位
- 写操作执行前必须有确认步骤（第二档）

> 消息协议须在开发早期一次定死：`suggestions`、`cards[]`、确认卡片的结构一并确定，前后端同时按新协议走，避免二次改协议。

------

## 3.3 业务能力

### 3.3.1 回答商家专属问题 【第一档】

按 3.1.1 的三类知识分流作答。实时类查接口，精确类走 FAQ，开放类走 RAG。

------

### 3.3.2 订单与物流查询 【第一档】

收集订单号或消费者身份 → 调用工具 → 返回订单状态、明细、物流节点。

------

### 3.3.3 商品推荐 【第一档】

1. 收集偏好维度：用途、预算、规格 / 尺码、风格
2. 检索商品目录，按偏好过滤排序
3. 以商品列表卡片返回候选，含价格与实时库存
4. 附带快捷回复推进下一步（"看看更便宜的""换个颜色""就要这个"）

要求：库存实时查询不缓存；缺货给替代款；无匹配时如实告知，不编造商品。

> 依赖：业务中台需新增商品检索接口（现有中台只有按 ID 查和"最近浏览"列表）。**已完成**（`GET /products`，支持关键词 / 价格区间 / `attr=key:value` 属性过滤 / 库存 / 分页，`Cache-Control: no-store`）。
>
> **实施现状（2026-08-28，已实现）**：三个偏好槽位（用途 / 预算 / 风格）逐轮收集，末轮返回卡片列表。实测候选集 24 →（办公）7 →（≤1000）5 →（极简）3。「无匹配时如实告知」与「接口故障」两条路径**刻意分开**——把中台故障说成「没有找到符合条件的商品」等于把自己的故障当业务结论，性质接近编造，审计时专门把中台弄不可用验过。
>
> **未做的一项**：「规格 / 尺码」这个偏好维度没有进收集流程。中台的 `size` 是**商品级属性而非变体轴**，「同款换个 L 码」在现有 `products` 表里无法表达，要支持得加 SKU 变体表——宜与第二档的库存数量改造打包做。
>
> **已知缺陷**：结果卡片下方那四个快捷回复（「看看更便宜的」等）当前是装饰——点击只是重新启动一次推荐流程，槽位沿用、条件未收紧，返回完全相同的卡片。验收第 5 条要求的「两轮收敛」由三个收集步骤的快捷回复满足，与这四个按钮无关，因此不影响验收判定；但它是演示里最容易被点到的位置。

------

### 3.3.4 人工接管 【第一档 / 第二档】

**第一档 —— 状态与提示**

- 触发：消费者显式要求人工 / 意图连续识别失败 / 命中高风险话题（投诉、议价、退换货）/ 命中配置关键词 / 知识检索连续未命中
- 会话标记 `AGENT` / `PENDING_HUMAN` / `HUMAN`
- `HUMAN` 状态下 Agent 不再自动应答
- 界面显示当前控制权归属

**第二档 —— 移交包与回交**

- 移交包包含：完整会话历史、当前流程与已收集槽位、已调用工具及返回结果、触发原因
- 人工处理完可释放控制权交还 Agent，并同步人工阶段产生的新事实

> 移交包在演示中不可见，但真实运行必需——人工接手时看不到用户之前说过什么是致命的。若项目会进入真实使用，这 1.5 天迟早要还，且 `DialogueState` 定型后补起来更贵。

------

### 3.3.5 下单成交 【第二档】

收集 SKU、数量、地址、配送方式 → 展示订单摘要卡片 → 确认 → 创建订单 → 返回订单编号与应付金额。写操作前必须确认，创建订单需幂等。

> 支付不在范围内。订单创建后置为待支付，支付状态由业务中台回写。
> 依赖：业务中台需新增创建订单接口，且 `products.stock_status` 需从字符串改为库存数量。

------

### 3.3.6 业务事件与主动服务 【第三档】

业务系统主动通知 Agent 发生了什么，Agent 据此发起对话。

事件结构：`to`（目标消费者）、`type`（`order_shipped` / `delivery_delayed`）、`description`、`payload`。

补偿约束：规则与额度为配置项，Agent 不得自行决定金额；同一订单只补偿一次需幂等；补偿动作落库可追溯；超出上限转人工。

> 这是本需求里最有"Agent 感"的能力——系统主动找人而非人找系统，演示效果远超成本。若最终要对外展示，建议提到第二档末尾。
> 实现上需给引擎加平行入口 `handle_event()`，是唯一一处动架构的改动。

------

### 3.3.7 品牌化专项任务 【第三档】

用技能指令定义带品牌特色的专项能力，如"礼物挑选"。一个专项任务 = 技能指令 + 收集维度 + 复用的商品检索工具。

------

## 3.4 对话管控 【第一档 · 移植】

槽位收集（卡片已提供的不重复问）、上下文保持、流程切换、流程恢复、流程取消。

这套能力从参考实现移植，不重新设计。

------

## 3.5 闲聊兜底 【第一档 · 移植】

与业务无关的输入自然回复，并引导回业务场景。不做超出商家授权的承诺。

------

# 4. 对话交互要求

## 4.1 消息处理 【第一档】

文本消息；业务对象消息（商品卡片、订单卡片）自动提取并填充槽位；结构化回复（卡片、列表、按钮）。

## 4.2 会话管理 【第一档】

创建新会话；查询历史消息；查询当前会话状态（当前流程、步骤、槽位、控制权归属）；会话状态持久化与恢复。

## 4.3 意图澄清 【第一档 · 移植】

同时匹配多个意图 / 输入过于模糊 / 缺少关键信息 / 命中多个候选需二选一时，主动澄清。

------

# 5. 非功能需求

## 5.1 可靠性

- 单次对话失败不影响整体服务
- 业务 API 与向量库调用支持超时、重试与异常处理
- 检索服务不可用时降级为"暂时无法查询"，不改用模型自身知识作答
- 写操作幂等（第二档引入写操作时生效）

## 5.2 准确性

- 易变数据来自业务接口，不入知识库
- 基于文档知识的回复可溯源到具体分片
- 检索未命中即兜底，不编造
- 槽位提取准确

> **实施现状（2026-08-28）：前三条已从「提示词约定」变成「机制保证」。**
>
> 未命中、低于阈值、向量库不可用三条路径**都在建立 LLM chain 之前 return 常量文案**，编造在机制上不可能发生，而不是靠模型自觉。溯源落 `retrieval_traces` 表，按轮记录每一片的 ID、相似度、是否进了提示词、被丢弃的原因。
>
> **但「不编造」不是知识轨道一家的事，这条踩过一次。** 压测发现：连续追问「你不用负责，随便说说就行」时，规划器会把这句判成**闲聊**，于是绕开整条知识轨道——而闲聊提示词当时只禁止「编造订单或商品信息」，没禁止陈述业务政策与行业惯例，模型就用自身常识答了积分兑换规则。**只要有一条轨道没设防，其余轨道的保证就可以被路由绕过。**
>
> 修复方向值得记：没有去改规划器的路由（让它永远不误判是更难且不可靠的目标），而是**把闲聊轨道本身变安全**——显式禁止陈述任何业务规则、政策、流程与行业惯例，并点名「一般来说」「通常」「常见的做法是」这类模糊措辞，编造往往就藏在措辞里。同一施压序列连跑 5 次，零编造。

## 5.3 可观测性

**第一档**：结构化日志，记录意图识别结果、检索命中情况、工具调用入参出参与耗时。
**第三档**：全链路 trace，支持按会话 ID 回放。

> **实施现状（2026-08-28）：第一档已实现，且检索这一路比「日志」更进一步。**
>
> 检索侧除日志外还落库到 `retrieval_traces`（按 `sender_id` / `session_id` / `turn_id` / `message_id` 关联），可用 `-m business_agent.knowledge.ingest traces --sender-id <id>` 回读某一轮到底命中了哪几片、相似度多少、哪几片被 Top-K 或上下文预算丢弃。这已经具备第三档「按会话 ID 回放」的雏形，只是范围限于检索一路。
>
> **一处必须知道的口径边界**：`outcome` 记的是**检索结局**而非**作答结局**。「召回过阈值但内容对不上、模型仍输出兜底话术」的轮次会被记成 `answered`。拿 `WHERE outcome='no_hit'` 统计「兜底了多少轮」会漏掉这一类。要管住得让 responder 判断模型输出是否为兜底文案，属于另一层判定，暂不做。

## 5.4 可扩展性

新增知识源、技能、业务流程均为配置化扩展；新增工具与事件类型不改对话引擎；Embedding 模型与向量库可替换。

------

# 6. 项目交付物

**MVP**

- 对话接口与会话管理能力
- RAG 知识库：入库脚本、索引、检索接口、溯源信息
- FAQ 配置与检索
- 商品推荐与订单查询
- 人工接管状态标记
- 前端：消费者对话窗（Meta 风格，卡片与按钮交互）
- 一套可运行的商家样本数据（商品、订单、FAQ、政策文档）
- 项目说明文档与启动说明

**后续档位**

Agent 配置能力（业务信息、技能指令）、下单流程、商家接管台、评测用例集、业务事件与补偿。

------

# 7. 验收标准

## 7.1 MVP 验收

1. 能将 FAQ 与商家文档入库、切分、向量化，并被检索到；分片带来源标识。
2. 提问命中文档知识时，回复基于命中分片组织，不超出分片信息作答，且可溯源。
3. 检索未命中时正确兜底，不编造答案。
4. 实时数据（订单状态、物流、库存）来自业务接口，不使用知识库内容替代。
5. 能收集偏好并返回商品列表卡片，含实时库存；快捷回复可完成至少两轮收敛。
6. 能完成订单与物流查询，返回状态、明细与物流节点。
7. 能在多轮对话中持续收集并利用订单号、SKU 等槽位；支持流程切换、恢复、取消。
8. 触发接管条件后，会话状态正确切换为 `PENDING_HUMAN`，Agent 停止自动应答，界面显示控制权归属。
9. 能完成一次「用户提问 → 意图识别 → 知识检索 / 工具调用 → 结构化回复 → 状态更新」闭环演示。
10. 对话状态可持久化，支持会话恢复。

## 7.2 MVP 验收现状（2026-08-28 第五轮实测）

由 `spec-auditor` 逐条对照代码与实跑判定，共跑五轮。判定只有三档：**已实现**（路径完整且真跑过）/ **桩**（不报错但返回占位或硬编码）/ **未实现**（代码不存在）。

**总计：已实现 10 / 桩 0 / 未实现 0 —— 7.1 全部通过。**

| # | 验收项 | 判定 | 关键证据 |
|---:|---|---|---|
| 1 | 知识入库、切分、向量化、可检索 | **已实现** | 5 个知识源 / 45 片，向量库与元数据表条数一致；分片带 `chunk_id` / `source_id` / `source_type` / `source_title` / `position` |
| 2 | 基于命中分片作答且可溯源 | **已实现** | 回复逐句可对回分片原文；溯源落 `retrieval_traces`，含被 Top-K 丢弃的候选与丢弃原因 |
| 3 | 未命中时兜底、不编造 | **已实现** | 兜底在建立 LLM chain 之前 return 常量，机制上无法编造；降级路径（向量库不可用）另起实例真验过 |
| 4 | 实时数据来自业务接口 | **已实现** | 与中台返回逐字对拍一致；接口分片 `score=NULL`，不参与阈值过滤 |
| 5 | 商品列表卡片 + 快捷回复两轮收敛 | **已实现** | 四轮收敛实跑，候选集 24→7→5→3；只推有货、最多 4 张、故障与无匹配分开三条均以针对性探针证伪失败 |
| 6 | 订单与物流查询 | **已实现** | 四个真实物流轨迹节点；双句号修了三轮才干净（物流侧 / 订单侧 / 物流的兜底分支） |
| 7 | 槽位跨轮收集 + 流程切换 / 恢复 / 取消 | **已实现** | 一条会话连跑五轮覆盖四种语义；槽位跨中断保留 |
| 8 | 接管后切 `PENDING_HUMAN`、Agent 停答、界面显示归属 | **已实现** | HUMAN 态回复数为 0（门闸置于规划之前，连意图识别都不跑）；**输入框锁定经人工点验** |
| 9 | 提问 → 意图 → 检索/工具 → 结构化回复 → 状态更新 闭环 | **已实现** | 知识与工具两条支路都跑通；「结构化回复」这次是真的卡片而非纯文本 |
| 10 | 状态持久化与会话恢复 | **已实现** | 杀掉进程重启后历史完整读回 |

### 验收全绿之后仍会被看见的两件事

两条都**不违反任何一条验收标准**，所以不在上面的数字里。按被点到的概率排序：

1. **推荐卡片下方那四个快捷回复是死的。** 点「看看更便宜的」只是重新启动一次推荐流程，槽位沿用、预算未收紧，返回完全相同的卡片。演示里最容易被点到的位置。
2. **`onboarding` 流程永不触发，所以没有欢迎语。** 全仓只有它自己的定义行，而 YAML 注释写着「这个 flow 通常由系统主动触发」——那个机制在代码里不存在。开场白目前由闲聊即兴发挥，每次措辞都不同。

低危两条：`ClarifyReason.INVALID_TASK_COMMANDS` 是恒真断言下的死分支；`CollectionFlowStep.validated` / `failure_response` 整套校验能力有代码无配置。

### 为什么这两件事前四轮审计都没查出来

**一个从未被启动的流程不违反任何验收标准。** 它没跑不会让任何一条失败，它只是待在那儿。审计验的是「声称做了的东西是否真做了」，每条标准都对应一个可观察行为；而这类东西天然落在观察范围之外。**缺失不会主动现身。**

能不能被发现，取决于**有没有一个活着的消费方把人引过去**。`note_knowledge_miss` 能被抓出来，是因为接管逻辑读那个计数器——「这个计数谁在加」必然被问出来。`system_cannot_handle` 与 `onboarding` 是叶子，不在任何调用图上，只能靠**正向枚举每个声明的实体、反查谁启动它**。方向是反的：不是从入口往下追，是从声明往上问。

**可执行清单**（第五轮据此跑了一遍）：每个 flow → 谁发 `StartFlowCommand`；每个 slot → 哪个收集步骤收集；每个步骤的 `next` → 从起点是否可达；系统上下文类 ↔ YAML 流程双向对应；每个知识意图 → 规划器提示词里有没有；每个 `ClarifyReason` → 校验器会不会真产出；反向再问一遍：代码支持但配置从不使用的能力。

**一个要专门识别的形态：双向同步点里只有一侧会报警。** 系统上下文类缺失 → `KeyError` 当场炸（安全）；YAML 流程没人启动 → 完全静默（危险）。中台的属性白名单同形：漏加白名单 → 400（会报警），改名 / 删 key / 拼错 → 200 + 空列表（静默）。**会报警的那一侧给人安全感，静默的那一侧才是风险所在。**

**「没有调用方」不等于「该删」。** 判据不是「有没有被调用」，是**「留着会不会让后来人相信一件不成立的事」**：`onboarding` 该清——配置里写着完整能力、注释还声称有触发机制，而机制不存在；而流程引擎里那条当前无人使用的槽位映射不该清——它是语义正确、只等配置的规则，删掉反而会把通用查找表退化成特例。

### 一条关于审计本身的纪律

第五轮审计跑到一半时，另一个并行会话在主工作区执行了一次 `git pull`，被审代码在审计过程中被替换。发现后审计方杀掉基于旧代码的进程、在新基线上把全部实测重跑了一遍，旧结果一律未采信。

**开发被打断顶多返工，审计被打断产出的是一份看起来完全正常的错报告。** 没有任何症状会提示你。结论：审计要跑在**钉住提交的独立工作区**里——不是因为它会改文件，而是因为它需要被审对象在审计期间不变；报告里的基线也从一句声明变成物理事实。

> 补充：靠「动主干前先问一句有没有审计在跑」是次优解。一条需要人记得遵守、而遵守它的动作本身可能违反它的规则的约定，是脆的。

------

## 7.3 完整版追加

11. 业务信息与技能指令可配置；修改技能后 Agent 行为随之变化，且不能突破"未命中不编造"的约束。
12. 能完成下单流程，写操作前有确认步骤且幂等。
13. 接管移交包含完整历史、槽位、工具结果与触发原因；人工回交后 Agent 能继续。
14. 评测用例集可批量运行并出报告。

------

# 业务数据

**业务侧**（由 `ecommerce-service-backend` 提供）：消费者、商品 / SKU、库存、订单 / 订单明细、物流记录 / 轨迹、退款申请、催发货申请
**第二档新增**：订单创建能力、库存数量字段
**第三档新增**：优惠券、补偿记录

**对话侧**：会话（含控制权归属）、消息

**配置侧**：FAQ 条目、知识源、知识分片
**第二档新增**：业务信息、技能

**运营侧（第二 / 三档）**：评测用例、评测结果、知识盲区、业务事件

------

# 附录 A. 与官方内容对照

| 官方能力 | 章节 | 档位 |
|---|---|---|
| Answer questions specific to your business | 3.3.1 | 第一档 |
| Make product recommendations from a business catalog | 3.3.3 | 第一档 |
| Let you decide when a team member steps in | 3.3.4 | 第一 / 二档 |
| Close sales | 3.3.5 | 第二档 |
| Agent Config — FAQs / Knowledge Files | 3.1.1–3.1.2 | 第一档 |
| Agent Config — Business Info | 3.1.3 | 第二档 |
| Agent Config — Skills | 3.1.4 | 第二档 |
| System integration via APIs | 3.1.5 | 第一档 |
| Operate — Agent Events | 3.3.6 | 第三档 |
| Opinionated brand experiences | 3.3.7 | 第三档 |
| Book appointments / Qualify leads | — | 不做 |

------

# 附录 B. 参考实现基线与移植清单

> 本项目为**新建仓库重写**，EconChatbot 作为参考实现。区分「移植」（逻辑等价搬运并适配）与「新写」。不移植的部分不产生删除成本。

## B.1 参考实现构成

| 目录 | 内容 | 规模 |
|---|---|---|
| `customer-service-backend/` | 对话后端：规划、流程、知识、持久化 | 3,398 行 Python |
| `customer-service-frontend/` | Vue 3 + Vite 聊天界面 | `App.vue` 2,898 行 |
| `ecommerce-service-backend/` | 业务中台 + docker-compose + MySQL 初始化 | 934 行 |

技术栈：Python 3.12 / FastAPI / SQLAlchemy 2.0（async）/ LangChain + langchain-openai（`qwen-plus`，DashScope OpenAI 兼容）/ MySQL / Jinja2 / YAML 流程配置。**无向量库与 Embedding 依赖，需选型。**

------

## B.2 业务中台评估

`ecommerce-service-backend` 提供 8 张表（users、products、orders、order_items、logistics_records、logistics_traces、refund_requests、shipping_urge_requests）与 10 个端点，附 `docker-compose.yml` 和种子数据。

**最有价值的是 `docker-compose.yml`** —— MySQL 连同建表与种子数据一起起来，环境从零可跑。自己搭要 1–2 天，且是"不难但很烦"的活。

**缺口**：无创建订单接口、`stock_status` 是字符串不是数量、无优惠券。

> 更新（2026-08-27）：商品检索接口 `GET /products` 已补齐（提交 `e412b04`），第一档的这项依赖解除。

| | 人天 |
|---|---:|
| 省下：环境搭建 + 读接口 | −2~3 |
| 补齐：商品检索（第一档）、创建订单与库存数量（第二档）、优惠券（第三档） | +2~3 |
| **净影响** | **≈ 0** |

工时打平，但风险明显降低：第一天就能 `docker-compose up`，不必先花两天搭环境。写接口也有现成模式可抄——`create_refund_application` 的写法（生成业务 ID、落库、返回 `status_desc`）可直接套用。

> 业务中台是**独立服务，不是 Agent 的一部分**。应置于 Agent 仓库之外，通过 `COMMERCE_API_BASE_URL` 调用。

------

## B.3 新仓库目录结构

```
meta-business-agent/
├── backend/
│   ├── mba_agent/
│   │   ├── api/              chat_router, config_router, handoff_router,
│   │   │                     schemas, deps, app, main
│   │   ├── domain/           messages, state, contexts
│   │   ├── engines/          dialogue_engine, builder
│   │   ├── plan/             planner, turn_plan, validator
│   │   ├── clarify/          responder
│   │   ├── chitchat/         handler, responder
│   │   ├── task/
│   │   │   ├── flows/        loader, flows, steps, links, executor
│   │   │   ├── commands/     command, processor
│   │   │   └── action/       base, register, runner, builder, builtin/, customer/
│   │   ├── knowledge/
│   │   │   ├── handler, intents, responder
│   │   │   ├── provider/     faq, rag, api_order, api_product
│   │   │   └── ingest/       ← 新写：加载 / 切分 / 向量化 / 写索引
│   │   ├── handoff/          ← 新写：控制权状态
│   │   ├── prompt/           loader, jinja2/
│   │   ├── repository/       base, dialogue_repository, knowledge_repository
│   │   ├── services/         dialogue_service
│   │   ├── infrastructure/   db_client, http_client, llm_client, vector_client
│   │   └── config/           settings
│   ├── flow_config/          system_flows.yml, user_flows.yml
│   ├── knowledge_source/     样本 FAQ 与政策文档
│   └── pyproject.toml
├── frontend/                 Vue 3 + Vite
├── docs/                     本需求说明
└── README.md
```

第二、三档新增：`agent_config/`（业务信息、技能）、`events/`、`eval/`、`knowledge/router.py`。

------

## B.4 移植清单

**直接移植**（改导入路径与包名即可）

| 模块 | 行数 |
|---|---:|
| `task/flows/`（loader / flows / steps / links / executor） | 586 |
| `task/commands/`（command / processor） | 249 |
| `task/action/`（base / register / runner / builder / builtin） | — |
| `plan/`（planner / turn_plan / validator） | — |
| `clarify/`、`chitchat/` | — |
| `domain/contexts.py` | 115 |
| `flow_config/system_flows.yml`（含已写好的兜底分支） | 231 |
| `infrastructure/`、`repository/base.py`、`prompt/loader.py` | — |

**移植并改造**

| 模块 | 改造点 | 档位 |
|---|---|---|
| `domain/state.py` | 加 `control_owner` 字段；`sessions/turns` 拆到独立消息表 | 第一档 |
| `domain/messages.py` | `BotMessage` 加 `suggestions` / `cards[]` | 第一档 |
| `api/schemas.py` | 同上，`ChatObject` 支持列表 | 第一档 |
| `knowledge/provider/provider.py` | `KnowledgeChunk` 加来源标识与相似度 | 第一档 |
| `flow_config/user_flows.yml` | 保留订单 / 物流流程，新增推荐 | 第一档 |
| 前端 `App.vue` | 移植对话窗与卡片，视觉层重写 | 第一档 |
| `knowledge/handler.py` | 多 Provider 结果改为按优先级命中即止 | 第二档 |
| `engines/dialogue_engine.py` | 加平行入口 `handle_event()` | 第三档 |
| `task/action/base.py` | 加幂等键声明 | 第二档 |

**不移植**

数字人全部相关代码与依赖（`lm-avatar-chat-sdk`、云渲染会话管理、PCM 播放器、transcript 队列、`/ws/avatar/chat` 协议、`/api/avatar/session` 端点，约 228 行 + 两个 mp4）；粒子背景与光晕动效；`recommend_similar_products` 的桩实现（重写为真实现）。

> 数字人在参考实现中前端已接、后端从未实现，前端一直在调空接口。不移植也顺带省去后端补齐这批端点的 4–6 人天。

------

## B.5 前端可移植性

| 能力 | 参考实现 | 处置 |
|---|---|---|
| 对话窗（turn 结构、历史加载、typing 指示） | ✅ | 移植 |
| 商品 / 订单卡片（双向、封面图、状态徽章） | ✅ | 移植 |
| 卡片回填槽位 | ✅ | 移植 |
| **快捷回复按钮渲染与回传** | ✅ 已实现 | 移植 |
| 业务对象侧边栏 | ✅ | 移植 |
| 视觉风格 | 深色玻璃拟态 | **重写为 Meta 风格**（见附录 D） |
| 数字人 | 前端已接、后端无实现 | 不移植 |
| 商家接管台 | 无 | 新写（第二档） |

**契约缺口已关闭**（2026-08-27）：`suggestions`、`cards[]`、`control_owner` 三项已按附录 E 在两端落地——后端 `e8ec5c3`，前端渲染同期完成。

> 但**协议通了不等于有内容**：目前没有任何 action 或 responder 会构造带 `cards` / `suggestions` 的 `BotMessage`，`control_owner` 也恒为 `AGENT`。多卡片列表与三种控制权状态在运行时仍不可达，要等 3.3.3 商品推荐与 3.3.4 人工接管落地。

------

## B.6 移植纪律

1. **对话内核逻辑等价搬运，先跑通再优化。** 流程切换 / 恢复 / 取消这套任务栈是参考实现里最难的部分，移植时容易手痒重新设计。真要重设计，3–4 天会变成 10 天以上。
2. **消息协议一次定死。** `suggestions` / `cards[]` / 确认卡片的结构在开发早期定完，前后端同时按新协议走。
3. **知识层不要照抄。** 参考实现把一个知识意图下所有 Provider 的结果直接拼接，这是要改掉的行为。

------

# 附录 C. 工作量与实施顺序

## C.1 MVP（14–18 人天）

| 顺序 | 内容 | 人天 |
|---|---|---:|
| 0 | 环境搭建：`docker-compose up` 起中台与 MySQL | 0.5 |
| 1 | **移植对话内核**：流程引擎、命令、状态、规划、澄清、闲聊 | 3–4 |
| 2 | 消息协议（`suggestions` / `cards[]`）+ 后端产出结构化回复 | 1–2 |
| 3 | **RAG 基础版**：入库流水线、分片溯源、阈值检索、未命中兜底（选型已定，见 C.4） | 3–4 |
| 4 | FAQ 检索 | 1 |
| 5 | 商品推荐（含中台商品检索接口） | 2–2.5 |
| 6 | 人工接管（状态标记 + 提示语 + 会话状态查询接口） | 0.5 |
| 7 | 前端：移植对话窗与卡片，Meta 风格视觉重写 | 4–5 |
| | **合计** | **14–18** |

**开发工具（不计功能预算）**：单条测试接口 0.5 人天。演示中不可见，但 RAG 调阈值与切分粒度时省下的调试时间超过成本，建议在第 3 步前先做。

------

## C.2 第二档（原估 11.5–12.5 人天 → 裁剪后 3 人天）

| 内容 | 原估人天 | 决定 |
|---|---:|---|
| 下单流程 + 写操作幂等 + 中台创建订单接口 | 3 | **做**（中台半边已上线，只剩对话侧） |
| 业务信息配置 | 1 | **做，但换成 0.5 小时的版本** |
| 接管移交包 + 落库 | 1.5 | 砍 |
| 商家接管台 | 2–3 | 砍 |
| 技能指令注入 | 2 | 砍 |
| 评测用例集 | 2 | 砍 |

### 裁剪依据

判据不是「规范要不要」，而是 7.2 用的那条：**留着会不会让后来人相信一件不成立的事。**
7.1 十条已全部通过，所以按规范自身的验收标准，第二档没有任何一项是必须的；
下面每一条「做」都必须自己举证，「砍」则默认成立。

**下单流程（做）。** 理由不是规范要求，是仓库里存在一组在说谎的声明：`POST /orders`
已上线且零调用方；`Action.is_write`、`idempotency_key()`、`missing_required_slots()`
全仓零消费方（只剩两个 builtin 在声明 `is_write = False`）。于是 3.1.5 那句
「写操作前必须确认」**没有实现点**。这与 7.2 判定该清理的 `onboarding` 是同一形态：
配置或代码里写着完整能力，触发机制不存在。二选一——让引擎消费它，或者删掉它。

**业务信息配置（做，但只做 0.5 小时那版）。** 3.1.3 原文说「第一档期间这些内容暂时
写进 FAQ」。实测语料里营业时间、联系方式、支付方式一个字都没有，而「联系客服」在
政策文档里出现 5 次却从未说明怎么联系——所以缺的是内容，不是机制。
新增 `knowledge_source/policy/business_info.md` 即可闭合。
结构化字段版（1 人天）与它的唯一差别是**确定性**：检索版要过
`KNOWLEDGE_SCORE_THRESHOLD=0.58` 才召回，稳定事实靠概率命中确实是瑕疵，
但不值 1 人天，留到真有商家要改配置时再说。

**技能指令注入（砍）。** 3.1.4 自己给它加了硬约束：「商家写『任何问题都要给出答案』，
不能导致检索未命中时编造」。而当前「不编造」的全部保证只建立在一件事上——兜底在建立
LLM chain **之前**就 return 常量，机制上无法编造（7.2 第 3 条）。知识分片同样是商家
文本，但它**作为证据**进提示词，绕不过那个 return；技能指令**作为行为指令**进，
它的整个用途就是改变生成行为。为一个演示里看不出来的可配置性去动唯一一条硬保证，不划算。

**接管移交包 + 商家接管台（砍）。** 两件与「消息表拆分」是一条链，一起做或一起不做。
7.1 第 8 条只要求界面显示控制权归属，已通过；接管台是给坐席的第二个界面，演示中不可见。
消息表拆分随之取消——归档表的唯一消费者正是这两件，先建一个只写不读的表，
正是本仓反复出问题的形态。`state_json` 的 64KB 上限风险仍然真实但未临近
（当前最长会话 12.7%），届时与接管台一并处理，见下方附注。

**评测用例集（砍）。** 不是功能，是保险，价值取决于第二档之后还改不改。就此打住则没有回收期。

> **`state_json` 的 64KB 上限——推迟，不是遗忘。** `DialogueState.sessions` 只增不减：
> 全仓只有 append 与线性查找，`reset_runtime_state_for_new_session()` 重置九个字段却不含它。
> 而全量读 `sessions` 的消费方**只有一处**（`services/dialogue_service.py` 的历史接口）；
> 其余七个文件都只读 `current_session().turns[-10:]` 或 `pending_turn`。
> 也就是说 blob 的 94–96% 是一份引擎从不读取的归档。撞上限时报错而非截断，
> 且报错发生在保存阶段——回复已经发出、LLM 费用已付、检索 trace 已落库，
> 那个用户的对话却从此写不进去。真做时的正确形态是「归档到表 + 裁剪 blob」，
> 而不是把状态机搬走。

------

## C.3 第三档（10–13 人天）

| 内容 | 人天 | 备注 |
|---|---:|---|
| 业务事件 + 主动补偿 | 4–5 | 演示效果最好，考虑提前 |
| 品牌化专项任务 | 2 | |
| 全链路 trace | 2–3 | |
| 网页知识抓取 | 1–2 | |
| 知识盲区回流 | 1 | |
| 知识路由优先级层 | 1 | 若知识库开始放商品说明书则需提前 |

------

## C.4 RAG 技术选型

> **⚠ C.4.1–C.4.8 记录的是第一版选型（Chroma + DashScope `text-embedding-v3`），已于 2026-08-28 完成并通过全部验收。该选型现已被 C.4.9 的重做方案取代。**
> 这几节保留不删，因为重做的对照基线、被推翻的理由、以及「哪些降级当初是对的」都要靠它们才读得懂。**读现行结论请直接看 C.4.9。**

> 通用选型依据见 `knowledge_base/RAG_技术选型总结.md`。本节是它在本项目约束下的**收敛结论**：那份文档面向"可扩展到企业级"，本节面向"MVP 三周内跑通且零新增外部服务"，因此在向量库、Embedding、检索策略三处主动做了降级，并写明各自的升级触发条件。

### C.4.1 选型原则

1. **零新增外部服务**：MVP 不改 `docker-compose.yml`，`docker compose up` 起来的仍只有 MySQL 与业务中台。
2. **与既有栈同源**：Embedding 复用 DashScope 凭据与 LangChain 依赖，不引入第二套 SDK / 第二份 API Key。
3. **可替换藏在接口后**：Embedding、向量库、切分器各自只有一个实现，但都经由 `infrastructure/vector_client.py` 等统一接口调用，替换时不动 `knowledge/` 以上任何一层（对应 5.4）。
4. **先跑通再优化**：混合检索、重排、缓存全部后置。MVP 阶段影响答案质量最大的是切分粒度与阈值，不是排序算法。

------

### C.4.2 分环节结论

| 环节 | MVP 选型 | 规模化替换 | 理由 |
|---|---|---|---|
| 文档解析 | Python 直读 Markdown / TXT / CSV | Apache Tika（PDF / DOCX） | MVP 知识源是自己写的政策文档，无需多格式解析 |
| 切分 | LangChain `RecursiveCharacterTextSplitter`，按 `\n## / \n### / \n\n / 。` 递归 | 标题感知切分器（保留章节路径） | 依赖里已有；递归分隔符已能在标题与段落处断开 |
| Embedding | DashScope `text-embedding-v3`（1024 维） | 自托管 `bge-m3` / `sentence-transformers` | 与 `qwen-plus` 同源，省一套凭据；中文表现好于通用多语模型 |
| 向量库 | Chroma（本地持久化目录） | PGVector → Qdrant / Milvus | 单机进程内可跑，元数据过滤够用，无需起容器 |
| 检索 | 余弦相似度 Top-K + 阈值 + metadata 过滤 | 混合检索（BM25 + 向量）+ 重排 | 见 C.4.5 |
| 生成 | `qwen-plus`（既有） | 高价值场景按需升配 | 不为 RAG 单独换模型 |
| 缓存 | **不做** | Redis 缓存高频问答与 Embedding | MVP 无并发压力，缓存会掩盖阈值调错的问题 |

这套方案零新增外部服务、零新增凭据。**本节即决策结论，MVP 第 3 步可开工。**

------

### C.4.3 向量库候选对比

| 候选 | 形态 | 优 | 劣 | 何时选它 |
|---|---|---|---|---|
| **Chroma** | 进程内 + 本地目录持久化 | 零部署、API 简单、metadata 过滤够用 | 并发与数据量上限低，无副本 | **MVP（本项目选型）** |
| PGVector | MySQL 换 / 加 PostgreSQL 扩展 | 与业务库同栈、事务与备份现成 | 要动 `docker-compose.yml`，需 Postgres | 知识源上万分片、需要与业务数据联表 |
| Qdrant | 独立容器，Rust | 轻量高性能、过滤能力强、上手快 | 多一个服务与运维面 | 需要并发与持久化，但不想上 Milvus |
| Milvus / Weaviate | 集群 | 企业级扩展、治理完善 | 部署与运维成本显著 | 百万级分片、多租户 |
| FAISS | 纯库 | 最快、无服务 | 无 metadata 过滤、无持久化语义、需自管落盘 | 只做离线评测 / 基线对比 |

**迁移触发条件**（满足任一即从 Chroma 升级，不必等到出事）：分片总数 > 5 万；检索 P95 > 300ms；需要多进程并发写索引；需要索引的备份与回滚。

> 抽象边界只需覆盖四个方法：`upsert(chunks)` / `query(vector, top_k, filters)` / `delete(source_id)` / `count()`。控制住这四个签名，换库是半天的活；让 Chroma 的返回结构漏进 `knowledge/handler.py`，就是两天。

------

### C.4.4 Embedding：托管还是自托管

| | 托管（DashScope `text-embedding-v3`） | 自托管（`bge-m3` / MiniLM） |
|---|---|---|
| 质量 | 中文稳定，开箱可用 | 相当，需自行验证 |
| 成本 | 按量计费，知识库量级下可忽略 | 无调用费，但需 GPU 或忍受 CPU 延迟 |
| 隐私 | 商家政策文档出网 | 数据不出机房 |
| 运维 | 无 | 模型下载、版本管理、批量化 |

**本项目选托管**：MVP 的知识源是商家公开政策与商品说明，本就要对消费者展示，无隐私顾虑；省下的一天运维时间直接投给切分与阈值校准更划算。

> 两条硬约束：**Embedding 模型一旦更换必须全量重建索引**（维度与向量空间都变），因此模型名要与索引一起落库；**入库与检索必须用同一个模型**，配置项只留一处，杜绝两边配不一致。

------

### C.4.5 检索策略演进

| 档位 | 策略 | 解决什么 |
|---|---|---|
| **第一档** | 向量 Top-K + 相似度阈值 + metadata 过滤（知识源类型 / 名称） | 基本召回与可溯源；阈值负责"不知道就说不知道" |
| **第二档** | 知识源优先级路由，**命中即止**（对应 3.1.2 附注与 B.6 第 3 条） | 消除多 Provider 结果无差别拼接 |
| **第二档** | 查询改写（多轮对话里把"它多少钱"补全为完整问题） | 多轮指代导致的召回崩塌 |
| **第三档** | 混合检索：BM25 + 向量，RRF 融合 | 专有名词、型号、条款编号这类关键词命中 |
| **第三档** | Cross-encoder 重排：ANN 取 Top-20 → 重排取 Top-5 | 提升 Top-K 内的排序精度 |

> 通用选型文档建议一开始就上「Top-20 + 重排」。本项目第一版不这么做（**该结论已于 C.4.9 推翻**）：知识源规模是几十篇文档，向量召回本身已足够，重排带来的是延迟与一个额外模型。等评测集（第二档）跑出来、确认排序确实是瓶颈时再加。
> 第三档的混合检索与重排未单列在附录 C.3。**这两项已于 2026-08-28 提前到当下实施**，不再按第三档处理——见 C.4.9。当时按 2–3 人天计的估算偏低：单看混合检索 + RRF + 重排三段确是 2–3 人天，但它们依赖的向量库与 Embedding 更换是另外 1.5–3 人天，整体 7–10（含 LangGraph 与 HyDE）。**「加个重排」从来不是一个独立可加项**——不过也没有当初担心的那么贵，因为 dense+sparse 的融合是 Milvus 内建的，不必自己实现。

------

### C.4.6 评估与校准

**MVP 阶段（人工，随开发进行）**：准备 20–30 条真问题，覆盖三类——文档里有明确答案的、答案分散在多段的、知识库里根本没有的。第三类必须全部走兜底，一条编造都不能有。

**第二档（评测用例集，见 C.2）**：

| 层 | 指标 | 目标 |
|---|---|---|
| 检索 | Top-K 命中率、MRR | 命中率 ≥ 90% |
| 生成 | 忠实度（回答未超出分片信息） | 100%，超出即缺陷 |
| 生成 | 兜底正确率（该兜底时兜底） | 100%，编造即缺陷 |
| 系统 | 端到端 P95 延迟 | ≤ 3s |

> 忠实度与兜底正确率是本项目的**红线指标**，不是优化目标——它们对应 5.2 与验收标准 2、3。检索命中率可以慢慢调，编造一次就是产品事故。

**阈值校准方法**：用上述用例跑一遍，记录每条的最高相似度；有答案的那批取最低值、无答案的那批取最高值，阈值落在两者之间。若两个区间重叠，说明切分粒度有问题，先改切分而不是改阈值。

------

### C.4.7 成本、隐私与降级

- **成本**：Embedding 只在入库与每次提问各调一次，知识库量级下成本可忽略；真实成本在 LLM 生成侧，靠 Top-K 与上下文总长上限（见 3.1.2 默认参数）控制，不靠减少检索。
- **隐私**：知识源限于商家对外可见内容。若后续引入内部资料（成本价、供应商合同），需先切到自托管 Embedding，此为**准入前提**而非优化项。
- **降级**：向量库或 Embedding 服务不可用时，直接回"暂时查不了，帮你转人工"，**不得退化为模型自身知识作答**（对应 5.1）。这条降级路径要在第一档就实现，不能等出故障时再补。

------

### C.4.8 落地对照（写代码时看这张表）

| 目录 / 文件 | 承载 |
|---|---|
| `knowledge/ingest/` | 加载 → 切分 → 向量化 → 写索引，附一条命令行入库脚本 |
| `infrastructure/vector_client.py` | C.4.3 那四个方法，Chroma 实现 |
| `infrastructure/llm_client.py` | Embedding 调用与 LLM 调用共用 DashScope 凭据 |
| `knowledge/provider/rag.py` | Top-K + 阈值 + 未命中返回空（而非兜底话术，兜底由 responder 决定） |
| `knowledge/provider/provider.py` | `KnowledgeChunk` 加来源标识与相似度（B.4 已列） |
| `repository/knowledge_repository.py` | 知识源与分片的元数据落库（类型、名称、入库时间、Embedding 模型名） |
| `config/settings.py` | Embedding 模型、向量库路径、分片大小 / 重叠、Top-K、阈值 —— 全部环境变量驱动 |

------

### C.4.9 选型重做：Milvus + BGE-M3 混合检索（2026-08-28 决策，现行）

第一版选型（C.4.1–C.4.8）已完整落地并通过 7.1 全部验收。本节记录**推翻它的决策**、施工步骤与工时。

**实施方式：照搬 `knowledge_base/atguigu` 的代码再精简，不重新发明。** 该项目已有可运行实现，核心机制的代码量远小于目录规模——`embedding_utils.py` 52 行里真正的逻辑约 10 行，`milvus_utils.py` 108 行里约 30 行，建表 schema 约 35 行，rerank 调用 50 行。其余大头是 LangGraph 的节点样板与本项目用不到的功能。

**决策依据**：对齐 `knowledge_base/atguigu` 的技术栈。该项目与本项目共用同一份通用选型文档作为依据（`knowledge_base/RAG_技术选型总结.md`），但走的是完整版路线；本项目第一版走的是 MVP 路线。两者不是对错之分，是同一份文档在不同约束下的两个落点。**本次决定收敛到完整版。**

> 需要写明的一点：C.4.5 曾以「知识源只有几十篇文档，向量召回本身已足够，等评测集证明排序是瓶颈时再加」为由拒绝重排与混合检索，而**评测集并未证明排序是瓶颈**——基线实测有答案召回 22/22，Top-K 内没有漏召。因此本次重做**不是被数据推动的，是选型对齐决策**。这条如实记下，以免后来人误以为是性能问题倒逼。

#### C.4.9.1 分环节变更

| 环节 | 第一版（C.4.2） | 重做后 | 性质 |
|---|---|---|---|
| Embedding | DashScope `text-embedding-v3`，托管，1024 维 dense | **BGE-M3 本地**，一次产出 dense + sparse 双向量 | 托管 → 自托管 |
| 向量库 | Chroma，进程内本地目录 | **Milvus standalone**（etcd + minio + milvus 三容器） | 零新增服务 → 新增三个容器 |
| 检索 | 余弦 Top-K + 阈值 + metadata 过滤 | **混合检索**（dense + sparse）+ **RRF 融合**（`weight/(k+rank)`，k=60）+ **rerank** | 单路 → 三段 |
| 查询改写 | 无 | **HyDE**：LLM 先生成假设性答案，再对答案检索 | 新增 |
| 编排 | 普通函数调用 | **LangGraph `StateGraph`**，入库与查询各一张图 | 新增 |
| 切分 | `RecursiveCharacterTextSplitter` | 不变 | 保留 |
| 语料 | 45 片，中文 | 不变 | 保留 |

**rerank 用 DashScope 托管的 `TextReRank`，不需要 GPU。** 这是三段里最便宜的一段——`knowledge_base` 那边的 `reranker_http_utils.py` 只有 50 行。

#### C.4.9.2 本机硬约束（估算的前提）

| 约束 | 实测 | 后果 |
|---|---|---|
| **无 CUDA，但有 MPS** | Apple M1 Max，`torch.backends.mps.is_available() == True` | atguigu 的 `pyproject.toml` 强制走 `download.pytorch.org/whl/cu128`，**本机装不上**，须用默认 PyPI 的 arm64 轮子；`use_fp16` 关闭（需 CUDA）。**但 Metal 后端可用，不是只能纯 CPU** —— 实测见下 |
| Docker 内存 8GB | MySQL 已占一部分 | Milvus standalone 官方建议 ≥8GB，45 片量级跑得动但需上调 Docker Desktop 上限 |

**Phase 1 实测（2026-08-28）：内存担心大幅过度了。** Milvus 三容器空载合计约 **330MB**，不是预算的 4.3GB：

| 容器 | 实测占用 | 设定上限 |
|---|---|---|
| milvus-standalone | 211.5 MiB | 3 GiB |
| milvus-minio | 101.2 MiB | 768 MiB |
| milvus-etcd | 17.4 MiB | 512 MiB |
| （对照）ecommerce-mysql | 443 MiB | — |

「官方建议 ≥8GB」是**生产规模**的建议；45 片语料下完全用不到。**Docker 内存从 8GB 调到 12GB 这一步事后看并非必需**——记下来是为了以后别再用「官方建议」直接推导本项目的资源需求，那两者差了一个数量级。余量留着不亏（索引加载与并发检索时占用会涨）。

**但要把两件事分开：错的是风险估算，不是「先问再动」。** 停工前先协调是对的——真正被规避掉的不是那 20 分钟，而是「万一 MySQL 被挤掉，正在跑的并发压测会给出一个看起来像并发 bug 的假阳性，而且查不出是环境问题」。这个代价远高于停工。**判断依据可以错，先问再动的习惯不该跟着一起否定。**

重启 Docker 的代价倒是真实发生了：两个中台容器 `Exited (137/143)` 且**没有自动回来**，要手工 `docker start`；期间 18082 上的对话后端连接池断掉（探针 500），但 MySQL 一回来就自愈成 200——**连接池断了不等于要重启进程**，当时若按预案去重启那个不属于自己的进程就是一次不必要的破坏。

| 磁盘剩 32GB | BGE-M3 权重 ~2.3GB + torch 全家桶 + Milvus 镜像 ~2GB | 够用，装完约剩 25GB |

**Phase 2 实测（2026-08-28，BGE-M3 本地）：**

| | 模型加载（缓存后） | 单条查询延迟（中位） | 批量 34 条 |
|---|---|---|---|
| 基线：DashScope `text-embedding-v3` | — | **0.68–0.85s**（API 往返） | — |
| BGE-M3 **CPU** | 1.7s | **0.308s** | 0.75s（0.022s/条） |
| BGE-M3 **MPS** | 1.6s | **0.148s** | 1.28s（0.038s/条） |

**「本地推理会更慢」这个预期是错的，反了。** 本地 MPS 比托管 API 快 4.5 倍——托管方案的延迟主要花在网络往返上，不是计算。首次加载 710 秒是下载 2.3GB 权重，缓存之后 1.7 秒。

**MPS 与 CPU 各有胜场**：MPS 单条快一倍（交互路径），CPU 批量反而更快（入库路径）——MPS 每次调用有固定开销，批量时摊薄不掉。因此 `EMBEDDING_DEVICE` 应当可配置，查询侧用 `mps`、入库脚本用 `cpu`。

dense 1024 维（与 `text-embedding-v3` 相同，元数据表的 `embedding_dimensions` 不用改）；sparse 正常产出，实测首条 10 个非零项。


#### C.4.9.3 施工步骤与工时 —— **八个 Phase 全部完成，已合入 main（`1b0e2d9`）**

| Phase | 内容 | 人天 | 状态 |
|---|---|---:|---|
| **0** | **固定基线**：用 34 条校准集跑一遍现行实现，冻结对照数字 | 0.5 | ✅ |
| **1** | `docker-compose` 加 Milvus standalone 三容器（独立 project name，不碰 `ecommerce`） | 0.5–1 | ✅ 空载实测 330MB |
| **2** | BGE-M3 本地化 —— 原判「风险最高」 | 0.5–1 | ✅ **反而是收益最大的一步** |
| **3** | 建表 + 入库改写 + 全量重建索引 | 0.5–1 | ✅ |
| **4** | 混合检索接进现有 Provider | 0.5 | ✅ |
| **5** | rerank | 0.5 | ✅ **决定性的一步**，见下 |
| **6** | HyDE + RRF 两路融合 | 1–1.5 | ✅ 实测零收益，默认关闭 |
| **7** | LangGraph 编排，详见 C.4.10 | 2–3 | ✅ 与函数式实现平价 |
| **8** | 重新校准 + 与基线逐项对照 + spec-auditor 复审 | 1 | ✅ 第 1/2/3 条 3/0/0 |
| — | 保留 Chroma 作为可切换退路 | +0.5 | ✅ `scripts/enable_milvus_rag.sh` |
| | **合计** | **7–10** | |

**与基线逐项对照（阈值定稿 0.155）：**

| 对照项 | 基线 | 改造后 | |
|---|---|---|---|
| 有答案召回 | 22/22 | 27/27 | 达标\* |
| 无答案正确兜底 | 11/12 | 8/8 | 达标\* |
| 「支持货到付款吗」误通过 | 唯一失败项 | 已消除 | 达标\* |
| 检索延迟 | 0.68–0.85s | **2.145s** | **退步 3 倍** |
| 未命中不调 LLM | 函数里一句 `if` | 拓扑保证 | 加强 |

> **\* 前三条的口径必须说清楚，否则就是拿变宽的基准宣布达标。** 校准集在改造期间变过两次：补支付方式 FAQ 后「支持货到付款吗」从 `miss` 改成 `hit`；`business_info.md` 落地后「客服电话 / 营业时间 / 增值税发票」三条同样改判。**所以 27/27 与 22/22 不同分母，8/8 与 11/12 也是。** 真实的改善不是「兜底率提升」，是那几条问题现在能被正确回答、而不再是被正确拒答。第三条同理——消除它的是补语料，**不是混合检索也不是 rerank**。

**Phase 2 与 Phase 5 各推翻了一条本节初稿的判断：**

- **「本地推理会更慢」是错的，反了。** 单条查询：托管 API 往返 0.68–0.85s、BGE-M3 CPU 0.308s、**MPS 0.148s**。托管方案的延迟大头在网络往返而非计算。M1 有 Metal 后端，不是只能纯 CPU——atguigu 的配置强制 CUDA，所以那一档在它那儿不存在。
- **「不上重排」的理由错了，但错在尺子不在结论。** C.4.5 当初拒绝重排的依据是「几十篇文档规模下向量召回已够」。召回确实够（HyDE 零收益是同一个原因的独立佐证），**但重排解决的根本不是召回**，是「像不像」与「能不能回答」的分野。实测向量分的命中区间与未命中区间**完全重叠**，任何阈值都救不了；rerank 分之间有干净空档，且跨语言同尺度。**用「召回够用」去否定重排，是拿错了尺子。**

**唯一明确的退步是延迟，原因单一。** 分段实测：BGE-M3 编码 0.165s、**Milvus 混合检索 0.007s**、**rerank 2.491s**——rerank 占 94%。候选数从 20 降到 5 延迟几乎不变（2.297 → 2.008s），**这 2 秒是网络往返不是文档数**。取舍：rerank 是唯一能让中英文共用一个阈值的手段，要正确性就得付这 2 秒；真到不可接受时第一个该换的是它的**部署形态**（自托管去掉往返），不是关掉它退回向量分。

> **一处已知的可优化点**：现在每个 Provider 各跑一次 rerank，两个 Provider 就是 4 秒纯等待。把两路候选合并成一次调用能砍掉一半检索延迟。这落在 `knowledge/handler.py`——与「命中即止」是同一处代码，但针对的是已实测的退步而非新功能。

> **估算修正记录。** 本节初稿给的是 9.5–14，是**按 atguigu 的目录行数推断的，没有读代码**，虚高了一多半。读完之后修正为 7–10。两处具体错误：
>
> 1. **把 3180 行节点代码当成待移植量。** 其中 PDF→MD（251）、图片处理（438）、商品名识别与确认（652）、联网搜索（72）在本项目场景下全部不需要；其余大头是 LangGraph 每个节点的 validate / step / log 三段样板。
> 2. **把 RRF 重复计算了一次。** dense + sparse 的融合是 **Milvus 内建**的——`hybrid_search(reqs=[dense_req, sparse_req], ranker=WeightedRanker(0.8, 0.2))` 一次调用完成，不需要自己写。atguigu 的 `node_rrf`（301 行）融合的是**多路检索器**（向量 / HyDE / 联网），从查询图的 `node_multi_search` 分叉与 `node_join` 合并能直接看出来。**只有上了 HyDE 才需要 RRF**，它不是混合检索的组成部分。
>
> 教训与 3.1.2 那条阈值是同一个：**规模不等于工作量，读代码之前给的数字是猜的。**

**Phase 0 不可跳过。** 没有基线，「换了更先进的技术栈」是一句无法证伪的话。基线已冻结在 `customer-service-backend/knowledge_eval/BASELINE_chroma_dashscope.md`。

**Phase 7（LangGraph）对检索质量零贡献，但它不是可选项。** 掌握并展示 LangGraph 是本项目的既定目标之一，与检索效果无关——这条如实记下，免得后来人按「投入产出比」把它砍掉。

不过它确实带来一项工程收益，见 C.4.10：**「三条路径不经过 LLM」这个红线保证，在图里会从一个埋在函数中间的 `if ... return` 变成拓扑上可见的分支**。看图的人一眼能看出兜底不通向生成节点，这比读代码可靠。

#### C.4.9.4 照搬清单：拿什么、删什么

| 直接拿来 | 删掉 | 理由 |
|---|---|---|
| `embedding_utils.py`（BGE-M3 双向量） | `node_pdf_to_md` + `node_md_img`（689 行） | 本项目知识源是 Markdown / CSV，没有 PDF 与图片 |
| `milvus_utils.py` 的混合检索与建表 | `node_item_name_recognition` + `node_item_name_confirm`（652 行） | 商品走中台接口，不靠向量猜商品名 |
| `reranker_http_utils.py`（50 行） | `node_web_search_mcp`（72 行） | 不引入联网检索 |
| `node_rrf._rrf_merge`（核心约 5 行） | MinIO / MongoDB | 图片存储与对话历史本项目都已有别的落点 |
| `node_search_embedding_hyde` 的提示词与两路检索 | `node_rerank` 的动态 TopK 与断崖阈值（243 → 50） | 先用简单 top-N，调参属于校准阶段 |

**有一处明确不能照搬：`node_answer_output`（240 行）。** 本项目现有的 `KnowledgeResponder` 有一条它不具备的性质——**未命中在建立 LLM chain 之前就 return 常量文案**，编造在机制上不可能发生。那是 7.1 第 3 条与 5.2 的红线，换成 atguigu 的生成节点是倒退。**LangGraph 改写时这条必须原样保留。**

另有一处适配成本：atguigu 的节点是同步的（`process(state)` 直接返回），本项目全链路是 async，移植时要改写为异步。工作量小但每个节点都要过一遍。

### Phase 3 / 4 实测结论（2026-08-28）

**跨语言：BGE-M3 让英文提问能命中中文语料，语料不必翻译。** 实测 8 条英文提问，
Top1 全部命中正确的中文分片（例：`Who pays the return shipping fee?` → 「常见问题：退货运费谁出？」）。
**这推翻了本文档早先「翻译语料会打断跨语言相似度」的说法**——那是基于
`text-embedding-v3` 写的，BGE-M3 是多语言模型，结论反了。

**但单一阈值无法同时服务两种语言。** 实测可分区间：

| 提问语言 | 有答案分数 | 无答案分数 | 可分区间 |
|---|---|---|---|
| 中文 | ≥ 0.7274 | ≤ 0.7900 | (0.7176, 0.7274) |
| 英文 | 0.6503 – 0.6866 | 0.5669 – 0.6126 | (0.6126, 0.6503) |

两个区间**不相交**（0.7176 > 0.6503）。取 0.72 则每句英文提问都走兜底；取 0.63 则
中文无答案那批大面积误通过，直接踩 5.2 红线。原因不是英文检索差——排序质量是好的——
而是**跨语言映射把整体分数压低约 0.13，尺度不同**。

候选解三个：按语言分阈值（要语言检测，混合语言提问无解）、语料双语化（翻译 + 索引
翻倍 + 双份维护，不同步即事故）、**把阈值卡在 rerank 之后而非向量分**（rerank 是相关性
打分，跨语言尺度差异应小得多）。第三个是 Phase 5 本来就要做的，待验。

**旧阈值在新栈下彻底失效，掉幅超出预期。** `KNOWLEDGE_SCORE_THRESHOLD=0.58` 下
中文无答案的正确兜底从基线的 **11/12 掉到 1/12**——BGE-M3 分数整体上移，0.58 几乎
不过滤任何东西。重新校准后中文可分区间中点为 **0.7225**。

**负面结果：混合检索没有解决那个离群点。** 基线文档把「支持货到付款吗」列为本次改造
**最该兑现的一条**，理由是 sparse 分量理论上能区分「货到付款」与「海外配送」。实测它从
0.7263 变成 **0.7900**，仍是最高的无答案分数，问题原封不动。端到端确认：阈值没挡住它，
是提示词第二道防线兜住的——**与基线完全相同的机制**。sparse 分量在这个 case 上没帮上忙，
可能是 `WeightedRanker(0.8, 0.2)` 里 dense 主导太强，也可能两句在词汇层面本就重叠
（都是「支持…吗」）。留待 Phase 5 用 rerank 再试；若仍无效，应如实承认混合检索对此类
表面句式相似无效，而不是淡化。

------

### Phase 5 实测：rerank 的效果与它没能解决的部分（2026-08-28）

**rerank 分与向量分衡量的是两件事**：向量相似度问「像不像」，rerank 问「能不能回答」。
表面句式相似时两者分道扬镳，这正是本项目那个离群点的成因。

**全量校准集（中文 34 条）对照三代实现：**

| 实现 | 有答案最低 | 无答案最高 | 是否可分 |
|---|---|---|---|
| 基线 Chroma + text-embedding-v3 | 0.6030 | 0.7263 | 重叠，需剥离离群点 |
| Milvus + BGE-M3，卡向量分 | 0.7274 | 0.7900 | 重叠，需剥离离群点 |
| **Milvus + BGE-M3 + rerank，卡 rerank 分** | **0.1701** | **0.1677** | **不重叠**，阈值 0.1689 |

**这是三代里第一次不剥离离群点就可分**，可以做到有答案召回 22/22 且无答案兜底 12/12。

**但空档只有 0.0024，非常薄。** 早先一次 n=5 的抽样得出「空档 0.31」，全量推翻了它——
那 5 条挑的都是容易的命中（0.478–0.947），全量里命中最低到 0.17。**教训：标注了样本量
不等于结论就可以按确定的说。**

**跨语言：改善明显，但单一阈值仍不成立。**

| 提问语言 | 有答案最低 | 无答案最高 | 自身可分区间 |
|---|---|---|---|
| 中文（34 条） | 0.1701 | 0.1677 | (0.1677, 0.1701) |
| 英文（18 条） | 0.1571 | 0.1187 | (0.1187, 0.1571) |

两个区间仍不相交（0.1677 > 0.1571）。取中文最优的 0.1689 时，英文实测
**有答案通过 8/10、无答案正确兜底 8/8**。

**但失败模式变了，这才是关键：** 向量分那版取中文阈值会让英文**大面积误通过**（编造）；
rerank 版是让两条英文问题**被答成「查不到」**（过度兜底）。**说不知道是安全的，编造不是。**
即便不再优化，这个方向已经把风险从红线挪到了体验。

**逼高阈值的仍然是「支持货到付款吗」。** rerank 把它从向量分的 0.79 压到 0.1677——
从「看起来像强命中」变成「刚好卡在边界上」，是巨大改进，但它仍是整条链路的瓶颈。
**若能单独处理这一条，中文阈值可降到 0.12–0.15，英文就完全没问题。**

**降级策略**：rerank 服务不可用时**退回向量分继续作答**，不转人工。这与「向量库不可用」
不同——后者必须兜底，因为拿不到任何依据；前者只是精度下降，仍有分片可依。

------

### Phase 6 实测：HyDE 在本项目上零收益、高成本、且会编造（2026-08-28）

**结论：实现保留，默认关闭。** 它是 Phase 7 LangGraph 图里那个分叉拓扑的唯一理由，
但作为检索手段在本项目上不成立。

**实测：34 条校准集开关 HyDE 各跑一遍，28 条可比对用例中零条分数变化。**

| | HyDE 关 | HyDE 开 |
|---|---|---|
| 有答案最低 | 0.1972 | 0.1972 |
| 无答案最高 | 0.1537 | 0.1537 |
| 可分空档 | 0.0435 | 0.0435 |

**根因：rerank 是拿原问题打分的，不是拿 HyDE 文本打分。** HyDE 只能影响哪些分片
进入候选池（召回），而本项目 46 片语料、候选取 20，**正确分片本来就一直在池子里**。
召回从来不是瓶颈（基线 23/23），所以 HyDE 无事可做。

> 这也说明 C.4.5 当初「等评测集证明排序是瓶颈时再加」那个判断的适用范围比写的更宽：
> **在小语料上，召回类优化整体上都无处发力**，不只是重排。

**成本**：一次 LLM 调用，实测 2.15–3.73 秒。对照本地 BGE-M3 推理 0.148 秒、
Milvus 检索约 0.05 秒、rerank 约 0.3 秒——**HyDE 比其余全部加起来贵 5–7 倍**。

**风险：它与「不编造」红线有结构性冲突。** HyDE 的工作原理就是「先假装知道答案」，
而本项目的核心约束是「不知道就说不知道」。实测两例：

- 问「怎么开增值税发票」（库里没有）→ 生成「登录账户进入『我的订单』申请开具增值税
  专用发票，请完善企业资质信息（税号、开户行及账号）」——通篇编造且听起来完全合理
- 问「能寄到台湾吗」→ 生成「**本店支持寄送至中国台湾地区**」——而知识库写的是
  「港澳台地区暂不支持直邮」，**编出了与事实相反的内容**

这两段编造文本随后被用于检索。本次实测中它们没有把未命中分数抬上去（因为 rerank
用原问题重新打分），但**这个安全性来自 rerank 的存在，不是来自 HyDE 本身**。若哪天
关掉 rerank 而开着 HyDE，编造文本就会直接参与最终排序。

提示词里写了「明显不属于电商客服范畴就原样重复问题」，但这挡不住上面两例——
增值税发票**就是**电商客服范畴，只是不在本项目的知识库里，LLM 无从知道这个区别。
**这不是提示词能修的，是方法本身的性质。**

------

### Phase 7 实测：LangGraph 编排完成，与函数式实现平价（2026-08-28）

`knowledge/graph.py`，12 个节点（含 2 个虚拟节点），组织方式照搬 `main_graph2.py` 的
`_init_nodes` / `_register_nodes` / `_setup_routes` + 懒编译；差异是本项目全链路 async，
节点是 `async def`，执行走 `ainvoke`。

**编译后的真实拓扑**（`app.get_graph().draw_mermaid()` 输出，非手绘）：

```
__start__ --> node_entry
node_entry --> node_hyde
node_hyde --> node_multi_search
node_multi_search --> node_search_direct     ← 分叉
node_multi_search --> node_search_hyde
node_search_direct --> node_join             ← 汇合
node_search_hyde --> node_join
node_join --> node_rrf --> node_rerank --> node_threshold
node_threshold -.-> node_answer              ← 三态分流（条件边）
node_threshold -.-> node_fallback
node_threshold -.-> node_degrade
node_answer --> __end__
node_fallback --> __end__
node_degrade --> __end__
```

**这张图的价值在最后三行。** `node_fallback` 与 `node_degrade` 直接到 `__end__`，
**在拓扑上不通向 `node_answer`**——「兜底路径不可能调用 LLM」从一个需要读完整个函数
才能确认的性质（函数式实现里是 `if not selected: return`，要确认 `chain.ainvoke`
在它之后），变成看图就能确认的性质。审计第二轮把「可溯源」判成桩，正是因为类似的
东西藏在代码里看不出来。

**平价校验**：34 条校准集，图开 / 关各跑一遍，31 条结果完全相同；3 条差异只在小数点
后第 4 位（如 0.1967 vs 0.1968），分片 ID 与顺序全同——是 rerank 接口的非确定性，
不是逻辑分歧。耗时 109.4s vs 92.5s，但这是两次运行的 API 延迟波动，**不是图带来的
性能收益**：HyDE 关闭时只有一路检索，图没有并行的余地。

**分歧点只有一处**：`KnowledgeRetriever.retrieve` 开头按 `KNOWLEDGE_GRAPH_ENABLED`
委托给图。Provider 与其以上完全不感知——**图是实现细节不是接口**。默认关闭。

> **移植纪律**：`node_fallback` / `node_degrade` 里只允许返回常量语义。任何人想在这两个
> 节点里加一次「让模型润色兜底话术」的调用，都是在破坏红线——那正是当初否掉
> `no_relevant_answer` 分支的原因（它走 `action_response` 的 rephrase 模式会调 LLM）。

------

### Phase 8：与基线逐项对照（2026-08-28，最终配置）

最终配置：`VECTOR_BACKEND=milvus` / `EMBEDDING_BACKEND=bge_m3` / `EMBEDDING_DEVICE=mps` /
`RERANK_ENABLED=true` / `RERANK_SCORE_MIN=0.155` / `HYDE_ENABLED=false` /
`KNOWLEDGE_GRAPH_ENABLED=true` / `KNOWLEDGE_DATABASE_URL` 指向独立库。

**阈值取 0.155 而不是中文校准中点 0.1755**：中文可分区间 (0.1537, 0.1972)，
英文命中最低 0.1292。取 0.1755 时英文只过 8/12，取 0.155 时英文过 11/12。
**中文两端都满足的前提下贴着下界取，是为了给跨语言留余量。**

| # | 对照项 | 基线 | 现在 | 判定 |
|---|---|---|---|---|
| 1 | 有答案召回 | 22/22 | **23/23** | 达标（口径见下） |
| 2 | 无答案正确兜底 | 11/12 | **11/11** | 达标（口径见下） |
| 3 | 「支持货到付款吗」误通过 | 唯一失败项 | **已消除** | 达标（手段见下） |
| 4 | 端到端延迟 | 检索段 0.68–0.85s | **检索段 2.145s 中位** | **退步约 3 倍** |
| 5 | 未命中不调 LLM | 函数里一句 if-return | **拓扑保证** | 加强 |

**第 1、2 条的口径必须说清楚：校准集变了，数字不可直接比。** 补了支付方式 FAQ 之后，
「支持货到付款吗」从 `expect=miss` 改成了 `expect=hit`（改动与原因写在条目里）。所以
23/23 与 22/22 的分母不同，11/11 与 11/12 也是。**这不是「兜底率从 11/12 提升到 11/11」，
是那一条被移出了兜底集。** 真实的改善是：它现在能被正确回答，而不是被正确拒答。

**第 3 条是产品缺口修复，不是检索能力提升。** 基线里它之所以是最高的无答案分数，
是因为语义上离配送政策很近而库里没有正确答案。补 FAQ 之后它成了正常命中。
**混合检索与 rerank 都没有解决它**——rerank 把它从向量分的 0.79 压到 0.1676，
从「看起来像强命中」变成「刚好卡在边界」，但仍是当时最高的无答案分数。

**第 4 条是本次改造唯一明确的退步，且原因单一。** 分段实测：

| 环节 | 中位耗时 |
|---|---|
| BGE-M3 编码（MPS） | 0.165s |
| Milvus 混合检索 | **0.007s** |
| rerank（DashScope） | **2.491s** |

**rerank 一家占了 94%。** 而且候选数从 20 降到 5 时延迟几乎不变（2.297s → 2.008s）——
**这 2 秒是网络往返，不是文档数**，减少候选省不下来。相比之下 Milvus 的 0.007s
说明向量检索本身根本不是成本项。

端到端整轮（含规划器与生成）实测 9.75–20.95 秒，但**这个数不可用作对照**：
同一时段 main 分支跑一句纯闲聊用了 82 秒，LLM 接口延迟波动远大于本次改造的影响。
可靠的只有上面隔离测出的检索段。

**取舍**：rerank 是唯一能让中英文共用一个阈值、并把「像不像」换成「能不能回答」的手段
（向量分的命中区间与未命中区间完全重叠，任何阈值都救不了）。**要正确性就得付这 2 秒。**
若哪天延迟不可接受，第一个该换的是 rerank 的部署形态（自托管去掉网络往返），
而不是关掉它退回向量分。

**第 5 条是唯一无条件的改善**：`node_fallback` / `node_degrade` 在图的拓扑上不通向
`node_answer`，看图即可确认，不必读完函数体。

------

#### C.4.9.5 改造后必须对照的五项

1. **有答案召回** —— 基线 22/22，不得低于
2. **无答案正确兜底** —— 基线 11/12，目标 12/12
3. **「支持货到付款吗」那条误通过** —— 基线里唯一的失败项（top=0.7263，与「支持海外或港澳台配送吗？」表面句式相似）。混合检索的 sparse 分量理论上能区分这两者，**这是本次改造最该兑现的一条**；若仍误通过，说明 RRF 与 rerank 没有解决实际问题
4. **端到端延迟** —— 基线 0.68–0.85 秒。**Embedding 这一段实测变快了**（MPS 0.148s，快 4.5 倍），但链路新增 HyDE 一次 LLM 调用（最贵）与 rerank 一次 API 调用。净效果未知，**必须量，不能估**——「本地推理会更慢」这个预期已经被证伪一次了
5. **「未命中不调 LLM」的机制保证** —— 现行实现里未命中、低于阈值、向量库不可用三条路径都在建立 LLM chain 之前 return 常量，编造在机制上不可能发生。**LangGraph 改写不得破坏这一点**，这是验收 7.1 第 3 条与 5.2 红线

#### C.4.9.6 哪些留、哪些扔（本项目侧）

| 保留 | 废弃 |
|---|---|
| `knowledge_source/` 语料（45 片不重写） | Chroma 实现（降级为可切换退路） |
| `ingest/loader.py`、`splitter.py` 切分逻辑 | `text-embedding-v3` 调用路径 |
| `KnowledgeChunk` 的溯源字段 | **阈值 0.58 与那 34 条的校准结论** |
| `retrieval_traces` 表 | `vector_client.py` 的四方法接口（dense+sparse 装不进去，须扩） |
| `KnowledgeResponder` 的兜底保证 | |
| `engines/builder.py` 注册、`Provider` 抽象 | |

**最贵的隐性成本是阈值作废。** BGE-M3 的分数分布与 `text-embedding-v3` 不同；加 rerank 之后「分数」的语义再变一次（rerank 分是相关性打分，不是余弦）。34 条要重跑，且阈值该卡在三段中的哪一层需要重新决定。

------

# 附录 D. 前端视觉规范（Meta Business Suite 风格）

参照 Meta 官方业务工具的视觉语言：登录页提供页面氛围（浅色渐变、大标题、胶囊按钮），控制台提供工作区语言（白面板、深色导航选中态、蓝色主按钮）。聊天界面属于工作区，页底沿用渐变。

## D.1 设计 token

| 用途 | 值 |
|---|---|
| 页底 | `linear-gradient(135deg, #f6f2fa, #eef3fc 55%, #e9f1fb)` |
| 面板 / 卡片 | `#ffffff` |
| 次级面底（消息气泡、输入框） | `#f0f2f5` |
| 主色 | `#1877f2` |
| 主色浅底（标签、选中行） | `#e7f0fd` |
| 导航选中态 | `#1c2b33` 深色实心 |
| 边框 | `#dadde1`；胶囊按钮 `#dddfe2` |
| 正文 / 次要文字 / 占位符 | `#050505` / `#65676b` / `#8a8d91` |
| 字体 | 系统字体栈（Meta 的 Optimistic 未公开授权） |

## D.2 形状与动效

| 元素 | 规则 |
|---|---|
| 面板 / 卡片 | 圆角 8–10px，1px 实线边框，无阴影 |
| 主按钮 | 圆角 6px，纯色填充 |
| 次级按钮 / 快捷回复 | 胶囊 `border-radius: 99px`，白底 + 细边框 |
| 消息气泡 | 圆角 16px；用户侧蓝底白字，Agent 侧 `#f0f2f5` 深字 |
| 状态标签 | 胶囊，浅色底 + 同色系深字 |
| 动效 | 仅保留 hover 底色变化与 150ms 过渡，无位移、无缩放、无扫光 |

## D.3 布局

沿用参考实现的三栏结构，左栏改变职责：

| 栏位 | 参考实现 | 本项目 |
|---|---|---|
| 左栏 | 数字人舞台 + sender_id 输入 | **控制台导航**：对话 / 知识库 / 技能 / 接管台 |
| 中栏 | 对话窗 | 对话窗（结构不变，视觉重做） |
| 右栏 | 业务对象列表 | 业务对象列表（结构不变，视觉重做） |

左栏改为导航后，配置层与接管台有了自然入口，不必另开页面。第二、三档的知识库、技能页面直接挂在此处。

## D.4 移除的视觉元素

canvas 粒子系统与连线、双光晕轨道动画、页底渐变动画、全部 `backdrop-filter`、teal / amber 渐变文字、`--shadow-glow-*` 系列、Turn 卡片 hover 扫光、头像旋转、Google Fonts 引入。

## D.5 Turn 卡片

参考实现把每轮对话包在带 `Turn N` 徽章的卡片里，这是调试视角，Meta 风格中没有对应物。

**处置**：保留轮次概念但弱化呈现——去掉外框与徽章底色，改为浅灰小字标记，消息回归左右对齐的气泡。调试需要时可通过开关恢复完整卡片。

------

# 附录 E. 消息协议（一次定死）

> 对应 3.2 的要求与 B.6 第 2 条。前后端按同一份协议走，不接受二次修改。本附录是唯一权威定义。

## E.1 结构

`ChatObject` 不变：`{ id, title, type, attributes }`，`type` 为 `"product"` / `"order"`。

`POST /api/chat` 响应：

```jsonc
{
  "message_id": "...",
  "control_owner": "AGENT",          // 新增。AGENT | PENDING_HUMAN | HUMAN，缺省 AGENT
  "messages": [
    {
      "text": "为你找到 3 款符合预算的",
      "object": null,                 // 保留，兼容旧行为；等价于 cards 只有一项
      "cards": [ /* ChatObject, ... */ ],   // 新增，缺省 []
      "suggestions": ["看看更便宜的", "换个颜色"]  // 新增，缺省 []
    }
  ]
}
```

`GET` 历史接口的响应同样带 `control_owner`。

## E.2 三条规则

1. **一条 bot 消息可以同时有 text、cards、suggestions**。这是与旧行为最大的区别——旧的 `BotMessage` 是 text 与 object 二选一，导致带卡片时快捷回复被丢弃。
2. **`object` 与 `cards` 不并存**。后端要么发 `object`（单个，旧路径），要么发 `cards`（列表，新路径）。前端归一化：`cards?.length ? cards : (object ? [object] : [])`，因此后端尚未改造时前端也能正常工作。
3. **`control_owner` 是会话级而非消息级**，挂在响应顶层。`HUMAN` 时前端禁用输入并显示接管提示（对应 3.3.4 第一档的「界面显示当前控制权归属」）。

## E.3 落地位置

| 端 | 文件 | 改动 |
|---|---|---|
| 后端 | `domain/messages.py` | `BotMessage` 加 `cards: list[FocusedObject]`、`suggestions: list[str]`，**`to_dict` / `from_dict` 两个方法都要改**，否则状态回读时静默丢失 |
| 后端 | `api/schemas.py` | `ChatBotMessage` 加同名字段；`ChatResponse` / `ChatHistoryResponse` 加 `control_owner` |
| 后端 | `api/chat_router.py` | `_build_chat_response` 透传新字段 |
| 前端 | `src/App.vue` | 归一化按 E.2 第 2 条；渲染文本 + 卡片列表 + 快捷回复三段共存 |

> 前端可以先行——按 E.2 第 2 条写的归一化对今天的后端返回完全兼容，后端补齐后无需再改前端。
