# Meta Business Agent 简易版 —— 需求说明

> 对标产品：Meta Business Agent（2026-06-03 发布）https://about.fb.com/news/2026/06/meta-business-agent/
> 架构参考：《The Architecture of Meta Business Agent on WhatsApp》（Meta Developers，2026-06-04）
> 配置模型参考：Meta Business Agent 完整指南（Standing Tech，2026-07-03）
> 参考实现：https://github.com/entingwu/EconChatbot/
> RAG 选型参考：`RAG_ref.md`（本仓库根目录，通用 RAG 技术选型总结）
>
> **交付策略**：新建仓库重写，先交付 MVP（14–18 人天），再按档位扩展。各功能的档位标记见每节标题。

---

## 1. 项目背景与目标

### 1.1 项目背景

商家通过消息渠道与消费者沟通。消息是 7×24 小时到达的，中小商家没有专职客服，人工无法覆盖。

Business Agent 与普通聊天机器人的区别在于：**它不是写死的问答脚本，而是一个可配置的 AI 助手**——商家把业务信息、FAQ、政策文档和行为指令配置进去，Agent 据此作答、办事，办不了时交给人。配置质量直接决定 Agent 质量。

------

### 1.2 官方架构的三根支柱

| 支柱 | 含义 | 章节 |
|---|---|---|
| Agent 配置 | Agent 知道什么、怎么回、能做什么 | 3.1 |
| 会话内体验 | 在对话里把事办完，不把人跳走 | 3.2 |
| 业务能力 | 答问、推荐、成交、接管 | 3.3 |

整体要求「接入既有技术栈」——Agent 不自建业务系统，通过接口调用商家现有的商品、订单服务。

------

### 1.3 三档优先级

| 档 | 判据 | 人天 |
|---|---|---:|
| **第一档** | 砍了就不能称为 Business Agent，且演示可见 | 6.5–9 |
| **第二档** | 真实运行需要，演示可后补 | 11.5–12.5 |
| **第三档** | 增强项，可不做 | 10–13 |

加上内核移植（3–4）与前端（4–5）：

| 方案 | 内容 | 合计 | 周期 |
|---|---|---:|---|
| **MVP** | 第一档 | **14–18** | 约 3 周 |
| 完整 | 一 + 二档 | 26–31 | 5.5 周 |
| 全量 | 三档全做 | 36–44 | 8 周 |

------

### 1.4 明确不做

真实渠道接入（用自建 Web 渠道模拟）、灰度与白名单、预约、线索资格认定、支付网关、多租户、计费、多语言、SSE、商家简报、退换货与投诉工单（走人工接管）、数字人。

------

# 2. 用户角色

**消费者**：咨询业务信息与政策、查订单、寻求商品推荐、寻求人工。

**商家 / 客服坐席**：接管 Agent 处理不了的会话。

**运营 / 开发者**：配置知识与技能指令，维护商品目录。

------

# 3. 功能需求

## 3.1 Agent 配置

### 3.1.1 知识源分类 【第一档】

三类知识源，适用场景不同，不可互相替代：

| 类型 | 内容 | 适用于 | 检索方式 |
|---|---|---|---|
| **实时业务数据** | 订单状态、物流进度、商品价格、库存 | 易变数据 | 调业务接口，不缓存 |
| **FAQ** | 高频问答对 | 需要精确可控答案的问题 | 精确 / 关键词匹配 |
| **文档知识** | 政策文档、商品说明、服务介绍 | 长文档，答案分散在段落里 | **RAG 检索** |

**配置纪律（替代路由层）**：易变数据一律不写入知识库。商品价格、库存、订单状态若被写进文档，会造成检索到的旧值与接口的新值并存。守住这条纪律，即可省去优先级路由层的实现（见 3.1.2 附注）。

------

### 3.1.2 RAG 检索 【第一档】

**入库**

- 文件类型：Markdown、TXT、CSV
- 支持知识源的新增、更新、删除，更新后索引同步生效
- 记录知识源的类型、名称、入库时间

**切分**

- 按语义边界切分，优先在标题与段落处断开
- 分片大小与重叠长度可配置
- FAQ 与 CSV 类结构化内容按条目切，不做语义切分

**向量化与索引**

- Embedding 模型可替换，通过配置指定
- 向量库可替换，通过统一接口抽象
- 每个分片保留来源标识：知识源 ID、标题、片段位置

**检索**

- 向量相似度检索，Top-K 可配置
- 设相似度阈值，低于阈值视为未命中
- 命中多个分片时按相似度排序取 Top-K 拼接进上下文

**生成约束**

- 回复必须基于命中分片组织，**不得超出分片信息作答**
- 内部记录命中的分片 ID 与相似度，回复可溯源

**不知道时怎么办**

- 未命中或低于阈值时，走兜底话术，说明需要什么信息，或引导转人工
- **禁止编造**，也不允许用模型自身通用知识替代商家知识

**默认参数**（全部可配置，选型与替换路径见附录 C.4）

| 参数 | 默认值 | 说明 |
|---|---|---|
| 分片大小 | 500–800 tokens | 语义边界优先，宁可偏小不可跨节 |
| 重叠长度 | 80–150 tokens | 约分片长度的 10–20% |
| 检索 Top-K | 5 | 拼接进上下文的分片上限 |
| 相似度阈值 | 余弦 0.35（**须用样本集校准**） | 低于阈值视为未命中，直接走兜底 |
| 上下文分片总长 | ≤ 2000 tokens | 超出按相似度从低到高截断 |
| FAQ / CSV 切分 | 一条一片，不做语义切分 | 条目本身即语义单元 |

> 阈值是唯一必须用真实语料校准的参数。拍脑袋定值会同时制造两类事故：该兜底的编造了，该回答的说不知道。附录 C.1 里那条「单条测试接口」正是为调阈值与切分粒度准备的，建议在 MVP 第 3 步开工前先做。

> 附注：知识源之间的优先级路由（命中即止）在第二档实现。第一档靠 3.1.1 的配置纪律规避冲突。

------

### 3.1.3 业务信息 【第二档】

营业时间、地址、联系方式、支付方式、退换货政策、配送说明等稳定事实，以结构化字段配置。第一档期间这些内容暂时写进 FAQ。

------

### 3.1.4 技能指令 【第二档】

针对特定情境的行为指令，三要素：

| 字段 | 说明 |
|---|---|
| `title` | 技能标识 |
| `description` | 何时启用 |
| `skill` | 具体行为指令 |

要求：以配置方式新增不改引擎；指令注入提示词模板不写死；只影响生成式回复的行为与语气，不改变知识来源与工具调用；**不得突破准确性约束**——商家写"任何问题都要给出答案"，不能导致检索未命中时编造。

------

### 3.1.5 工具与业务系统集成 【第一档】

- Agent 不自建业务系统，通过工具调用商家既有服务
- 工具统一注册，新增工具不改对话引擎
- 每个工具声明：名称、入参、出参、是否为写操作
- 写操作声明幂等键（第二档引入写操作时生效）
- 工具调用失败时如实告知，不用其他信息替代

**第一档工具**：查订单、查物流、查商品、检索商品目录
**第二档新增**：创建订单
**第三档新增**：发放优惠券

------

## 3.2 会话内体验 【第一档】

核心原则：在对话里把事办完，不把消费者跳走。

| 结构化消息 | 用途 | 档位 |
|---|---|---|
| 快捷回复按钮 | 收敛提问、推进成交 | 第一档 |
| 商品卡片 / 列表 | 推荐结果展示与多选一 | 第一档 |
| 订单卡片 | 订单信息展示 | 第一档 |
| 订单摘要确认卡片 | 下单前确认 | 第二档 |

- Agent 可发出业务对象消息；消费者点击后该对象回传并自动填充槽位
- 写操作执行前必须有确认步骤（第二档）

> 消息协议须在开发早期一次定死：`suggestions`、`cards[]`、确认卡片的结构一并确定，前后端同时按新协议走，避免二次改协议。

------

## 3.3 业务能力

### 3.3.1 回答商家专属问题 【第一档】

按 3.1.1 的三类知识分流作答。实时类查接口，精确类走 FAQ，开放类走 RAG。

------

### 3.3.2 订单与物流查询 【第一档】

收集订单号或消费者身份 → 调用工具 → 返回订单状态、明细、物流节点。

------

### 3.3.3 商品推荐 【第一档】

1. 收集偏好维度：用途、预算、规格 / 尺码、风格
2. 检索商品目录，按偏好过滤排序
3. 以商品列表卡片返回候选，含价格与实时库存
4. 附带快捷回复推进下一步（"看看更便宜的""换个颜色""就要这个"）

要求：库存实时查询不缓存；缺货给替代款；无匹配时如实告知，不编造商品。

> 依赖：业务中台需新增商品检索接口（现有中台只有按 ID 查和"最近浏览"列表）。

------

### 3.3.4 人工接管 【第一档 / 第二档】

**第一档 —— 状态与提示**

- 触发：消费者显式要求人工 / 意图连续识别失败 / 命中高风险话题（投诉、议价、退换货）/ 命中配置关键词 / 知识检索连续未命中
- 会话标记 `AGENT` / `PENDING_HUMAN` / `HUMAN`
- `HUMAN` 状态下 Agent 不再自动应答
- 界面显示当前控制权归属

**第二档 —— 移交包与回交**

- 移交包包含：完整会话历史、当前流程与已收集槽位、已调用工具及返回结果、触发原因
- 人工处理完可释放控制权交还 Agent，并同步人工阶段产生的新事实

> 移交包在演示中不可见，但真实运行必需——人工接手时看不到用户之前说过什么是致命的。若项目会进入真实使用，这 1.5 天迟早要还，且 `DialogueState` 定型后补起来更贵。

------

### 3.3.5 下单成交 【第二档】

收集 SKU、数量、地址、配送方式 → 展示订单摘要卡片 → 确认 → 创建订单 → 返回订单编号与应付金额。写操作前必须确认，创建订单需幂等。

> 支付不在范围内。订单创建后置为待支付，支付状态由业务中台回写。
> 依赖：业务中台需新增创建订单接口，且 `products.stock_status` 需从字符串改为库存数量。

------

### 3.3.6 业务事件与主动服务 【第三档】

业务系统主动通知 Agent 发生了什么，Agent 据此发起对话。

事件结构：`to`（目标消费者）、`type`（`order_shipped` / `delivery_delayed`）、`description`、`payload`。

补偿约束：规则与额度为配置项，Agent 不得自行决定金额；同一订单只补偿一次需幂等；补偿动作落库可追溯；超出上限转人工。

> 这是本需求里最有"Agent 感"的能力——系统主动找人而非人找系统，演示效果远超成本。若最终要对外展示，建议提到第二档末尾。
> 实现上需给引擎加平行入口 `handle_event()`，是唯一一处动架构的改动。

------

### 3.3.7 品牌化专项任务 【第三档】

用技能指令定义带品牌特色的专项能力，如"礼物挑选"。一个专项任务 = 技能指令 + 收集维度 + 复用的商品检索工具。

------

## 3.4 对话管控 【第一档 · 移植】

槽位收集（卡片已提供的不重复问）、上下文保持、流程切换、流程恢复、流程取消。

这套能力从参考实现移植，不重新设计。

------

## 3.5 闲聊兜底 【第一档 · 移植】

与业务无关的输入自然回复，并引导回业务场景。不做超出商家授权的承诺。

------

# 4. 对话交互要求

## 4.1 消息处理 【第一档】

文本消息；业务对象消息（商品卡片、订单卡片）自动提取并填充槽位；结构化回复（卡片、列表、按钮）。

## 4.2 会话管理 【第一档】

创建新会话；查询历史消息；查询当前会话状态（当前流程、步骤、槽位、控制权归属）；会话状态持久化与恢复。

## 4.3 意图澄清 【第一档 · 移植】

同时匹配多个意图 / 输入过于模糊 / 缺少关键信息 / 命中多个候选需二选一时，主动澄清。

------

# 5. 非功能需求

## 5.1 可靠性

- 单次对话失败不影响整体服务
- 业务 API 与向量库调用支持超时、重试与异常处理
- 检索服务不可用时降级为"暂时无法查询"，不改用模型自身知识作答
- 写操作幂等（第二档引入写操作时生效）

## 5.2 准确性

- 易变数据来自业务接口，不入知识库
- 基于文档知识的回复可溯源到具体分片
- 检索未命中即兜底，不编造
- 槽位提取准确

## 5.3 可观测性

**第一档**：结构化日志，记录意图识别结果、检索命中情况、工具调用入参出参与耗时。
**第三档**：全链路 trace，支持按会话 ID 回放。

## 5.4 可扩展性

新增知识源、技能、业务流程均为配置化扩展；新增工具与事件类型不改对话引擎；Embedding 模型与向量库可替换。

------

# 6. 项目交付物

**MVP**

- 对话接口与会话管理能力
- RAG 知识库：入库脚本、索引、检索接口、溯源信息
- FAQ 配置与检索
- 商品推荐与订单查询
- 人工接管状态标记
- 前端：消费者对话窗（Meta 风格，卡片与按钮交互）
- 一套可运行的商家样本数据（商品、订单、FAQ、政策文档）
- 项目说明文档与启动说明

**后续档位**

Agent 配置能力（业务信息、技能指令）、下单流程、商家接管台、评测用例集、业务事件与补偿。

------

# 7. 验收标准

## 7.1 MVP 验收

1. 能将 FAQ 与商家文档入库、切分、向量化，并被检索到；分片带来源标识。
2. 提问命中文档知识时，回复基于命中分片组织，不超出分片信息作答，且可溯源。
3. 检索未命中时正确兜底，不编造答案。
4. 实时数据（订单状态、物流、库存）来自业务接口，不使用知识库内容替代。
5. 能收集偏好并返回商品列表卡片，含实时库存；快捷回复可完成至少两轮收敛。
6. 能完成订单与物流查询，返回状态、明细与物流节点。
7. 能在多轮对话中持续收集并利用订单号、SKU 等槽位；支持流程切换、恢复、取消。
8. 触发接管条件后，会话状态正确切换为 `PENDING_HUMAN`，Agent 停止自动应答，界面显示控制权归属。
9. 能完成一次「用户提问 → 意图识别 → 知识检索 / 工具调用 → 结构化回复 → 状态更新」闭环演示。
10. 对话状态可持久化，支持会话恢复。

## 7.2 MVP 验收现状（2026-08-27 实测）

由 `spec-auditor` 逐条对照代码与实跑判定。判定只有三档：**已实现**（路径完整且真跑过）/ **桩**（不报错但返回占位或硬编码）/ **未实现**（代码不存在）。

**总计：已实现 5 / 桩 4 / 未实现 1**

| # | 验收项 | 判定 | 卡在哪 |
|---:|---|---|---|
| 1 | 知识入库、切分、向量化、可检索 | **未实现** | 全仓无 embedding / 向量库依赖，无入库端点，无知识源与分片表 |
| 2 | 基于命中分片作答且可溯源 | **桩** | 作答链路真在跑，但喂进去的是占位字符串；`KnowledgeChunk` 只有 `content`，无溯源字段 |
| 3 | 未命中时兜底、不编造 | **桩** | 行为上没编造，但不是判定出来的——Provider 永远返回占位串。无阈值、无命中判定 |
| 4 | 实时数据来自业务接口 | **已实现** | — |
| 5 | 商品列表卡片 + 快捷回复两轮收敛 | **桩** | 协议、前端渲染、中台 `GET /products` 三样就位，中间无人调用，`cards[]` 恒为空 |
| 6 | 订单与物流查询 | **已实现** | — |
| 7 | 槽位跨轮收集 + 流程切换 / 恢复 / 取消 | **已实现** | 完成度最高的一段 |
| 8 | 接管后切 `PENDING_HUMAN`、Agent 停答、界面显示归属 | **桩** | 前端三态 UI 已就绪；后端 `control_owner` 写死 `AGENT`，无状态机、无停答分支 |
| 9 | 提问 → 意图 → 检索/工具 → 结构化回复 → 状态更新 闭环 | **已实现** | 仅工具调用这一支；「结构化回复」是槽位渲染的文本而非卡片，随第 5 条一起转好 |
| 10 | 状态持久化与会话恢复 | **已实现** | — |

**按对演示的影响排序，还差三块：**

1. **整条 RAG（第 1 / 2 / 3 条）** —— 唯一的「未实现」在这里，且三条连锁：没有入库与分片，溯源与命中判定都无从谈起。演示时问一句退货政策就会回「暂未查询到」。需要先定选型（见附录 C.4）。
2. **商品卡片与快捷回复（第 5 条）** —— 补齐成本最低、视觉落差最明显。三样基础设施都已就位，缺的只是「偏好槽位 + 调 `GET /products` + 写进 `cards[]`」这一段。
3. **人工接管（第 8 条）** —— 前端已完全就绪，后端点不亮。演示脚本里若有转人工这一幕会直接穿帮。

> 第 4、6、7、10 条扎实可演。**第 7 条建议作为演示主线**——槽位跨越流程打断与恢复仍然留存，是当前完成度最高、最能体现「多轮对话管控」的一段。

------

## 7.3 完整版追加

11. 业务信息与技能指令可配置；修改技能后 Agent 行为随之变化，且不能突破"未命中不编造"的约束。
12. 能完成下单流程，写操作前有确认步骤且幂等。
13. 接管移交包含完整历史、槽位、工具结果与触发原因；人工回交后 Agent 能继续。
14. 评测用例集可批量运行并出报告。

------

# 业务数据

**业务侧**（由 `ecommerce-service-backend` 提供）：消费者、商品 / SKU、库存、订单 / 订单明细、物流记录 / 轨迹、退款申请、催发货申请
**第二档新增**：订单创建能力、库存数量字段
**第三档新增**：优惠券、补偿记录

**对话侧**：会话（含控制权归属）、消息

**配置侧**：FAQ 条目、知识源、知识分片
**第二档新增**：业务信息、技能

**运营侧（第二 / 三档）**：评测用例、评测结果、知识盲区、业务事件

------

# 附录 A. 与官方内容对照

| 官方能力 | 章节 | 档位 |
|---|---|---|
| Answer questions specific to your business | 3.3.1 | 第一档 |
| Make product recommendations from a business catalog | 3.3.3 | 第一档 |
| Let you decide when a team member steps in | 3.3.4 | 第一 / 二档 |
| Close sales | 3.3.5 | 第二档 |
| Agent Config — FAQs / Knowledge Files | 3.1.1–3.1.2 | 第一档 |
| Agent Config — Business Info | 3.1.3 | 第二档 |
| Agent Config — Skills | 3.1.4 | 第二档 |
| System integration via APIs | 3.1.5 | 第一档 |
| Operate — Agent Events | 3.3.6 | 第三档 |
| Opinionated brand experiences | 3.3.7 | 第三档 |
| Book appointments / Qualify leads | — | 不做 |

------

# 附录 B. 参考实现基线与移植清单

> 本项目为**新建仓库重写**，EconChatbot 作为参考实现。区分「移植」（逻辑等价搬运并适配）与「新写」。不移植的部分不产生删除成本。

## B.1 参考实现构成

| 目录 | 内容 | 规模 |
|---|---|---|
| `customer-service-backend/` | 对话后端：规划、流程、知识、持久化 | 3,398 行 Python |
| `customer-service-frontend/` | Vue 3 + Vite 聊天界面 | `App.vue` 2,898 行 |
| `ecommerce-service-backend/` | 业务中台 + docker-compose + MySQL 初始化 | 934 行 |

技术栈：Python 3.12 / FastAPI / SQLAlchemy 2.0（async）/ LangChain + langchain-openai（`qwen-plus`，DashScope OpenAI 兼容）/ MySQL / Jinja2 / YAML 流程配置。**无向量库与 Embedding 依赖，需选型。**

------

## B.2 业务中台评估

`ecommerce-service-backend` 提供 8 张表（users、products、orders、order_items、logistics_records、logistics_traces、refund_requests、shipping_urge_requests）与 10 个端点，附 `docker-compose.yml` 和种子数据。

**最有价值的是 `docker-compose.yml`** —— MySQL 连同建表与种子数据一起起来，环境从零可跑。自己搭要 1–2 天，且是"不难但很烦"的活。

**缺口**：无创建订单接口、`stock_status` 是字符串不是数量、无优惠券。

> 更新（2026-08-27）：商品检索接口 `GET /products` 已补齐（提交 `e412b04`），第一档的这项依赖解除。

| | 人天 |
|---|---:|
| 省下：环境搭建 + 读接口 | −2~3 |
| 补齐：商品检索（第一档）、创建订单与库存数量（第二档）、优惠券（第三档） | +2~3 |
| **净影响** | **≈ 0** |

工时打平，但风险明显降低：第一天就能 `docker-compose up`，不必先花两天搭环境。写接口也有现成模式可抄——`create_refund_application` 的写法（生成业务 ID、落库、返回 `status_desc`）可直接套用。

> 业务中台是**独立服务，不是 Agent 的一部分**。应置于 Agent 仓库之外，通过 `COMMERCE_API_BASE_URL` 调用。

------

## B.3 新仓库目录结构

```
meta-business-agent/
├── backend/
│   ├── mba_agent/
│   │   ├── api/              chat_router, config_router, handoff_router,
│   │   │                     schemas, deps, app, main
│   │   ├── domain/           messages, state, contexts
│   │   ├── engines/          dialogue_engine, builder
│   │   ├── plan/             planner, turn_plan, validator
│   │   ├── clarify/          responder
│   │   ├── chitchat/         handler, responder
│   │   ├── task/
│   │   │   ├── flows/        loader, flows, steps, links, executor
│   │   │   ├── commands/     command, processor
│   │   │   └── action/       base, register, runner, builder, builtin/, customer/
│   │   ├── knowledge/
│   │   │   ├── handler, intents, responder
│   │   │   ├── provider/     faq, rag, api_order, api_product
│   │   │   └── ingest/       ← 新写：加载 / 切分 / 向量化 / 写索引
│   │   ├── handoff/          ← 新写：控制权状态
│   │   ├── prompt/           loader, jinja2/
│   │   ├── repository/       base, dialogue_repository, knowledge_repository
│   │   ├── services/         dialogue_service
│   │   ├── infrastructure/   db_client, http_client, llm_client, vector_client
│   │   └── config/           settings
│   ├── flow_config/          system_flows.yml, user_flows.yml
│   ├── knowledge_source/     样本 FAQ 与政策文档
│   └── pyproject.toml
├── frontend/                 Vue 3 + Vite
├── docs/                     本需求说明
└── README.md
```

第二、三档新增：`agent_config/`（业务信息、技能）、`events/`、`eval/`、`knowledge/router.py`。

------

## B.4 移植清单

**直接移植**（改导入路径与包名即可）

| 模块 | 行数 |
|---|---:|
| `task/flows/`（loader / flows / steps / links / executor） | 586 |
| `task/commands/`（command / processor） | 249 |
| `task/action/`（base / register / runner / builder / builtin） | — |
| `plan/`（planner / turn_plan / validator） | — |
| `clarify/`、`chitchat/` | — |
| `domain/contexts.py` | 115 |
| `flow_config/system_flows.yml`（含已写好的兜底分支） | 231 |
| `infrastructure/`、`repository/base.py`、`prompt/loader.py` | — |

**移植并改造**

| 模块 | 改造点 | 档位 |
|---|---|---|
| `domain/state.py` | 加 `control_owner` 字段；`sessions/turns` 拆到独立消息表 | 第一档 |
| `domain/messages.py` | `BotMessage` 加 `suggestions` / `cards[]` | 第一档 |
| `api/schemas.py` | 同上，`ChatObject` 支持列表 | 第一档 |
| `knowledge/provider/provider.py` | `KnowledgeChunk` 加来源标识与相似度 | 第一档 |
| `flow_config/user_flows.yml` | 保留订单 / 物流流程，新增推荐 | 第一档 |
| 前端 `App.vue` | 移植对话窗与卡片，视觉层重写 | 第一档 |
| `knowledge/handler.py` | 多 Provider 结果改为按优先级命中即止 | 第二档 |
| `engines/dialogue_engine.py` | 加平行入口 `handle_event()` | 第三档 |
| `task/action/base.py` | 加幂等键声明 | 第二档 |

**不移植**

数字人全部相关代码与依赖（`lm-avatar-chat-sdk`、云渲染会话管理、PCM 播放器、transcript 队列、`/ws/avatar/chat` 协议、`/api/avatar/session` 端点，约 228 行 + 两个 mp4）；粒子背景与光晕动效；`recommend_similar_products` 的桩实现（重写为真实现）。

> 数字人在参考实现中前端已接、后端从未实现，前端一直在调空接口。不移植也顺带省去后端补齐这批端点的 4–6 人天。

------

## B.5 前端可移植性

| 能力 | 参考实现 | 处置 |
|---|---|---|
| 对话窗（turn 结构、历史加载、typing 指示） | ✅ | 移植 |
| 商品 / 订单卡片（双向、封面图、状态徽章） | ✅ | 移植 |
| 卡片回填槽位 | ✅ | 移植 |
| **快捷回复按钮渲染与回传** | ✅ 已实现 | 移植 |
| 业务对象侧边栏 | ✅ | 移植 |
| 视觉风格 | 深色玻璃拟态 | **重写为 Meta 风格**（见附录 D） |
| 数字人 | 前端已接、后端无实现 | 不移植 |
| 商家接管台 | 无 | 新写（第二档） |

**契约缺口已关闭**（2026-08-27）：`suggestions`、`cards[]`、`control_owner` 三项已按附录 E 在两端落地——后端 `e8ec5c3`，前端渲染同期完成。

> 但**协议通了不等于有内容**：目前没有任何 action 或 responder 会构造带 `cards` / `suggestions` 的 `BotMessage`，`control_owner` 也恒为 `AGENT`。多卡片列表与三种控制权状态在运行时仍不可达，要等 3.3.3 商品推荐与 3.3.4 人工接管落地。

------

## B.6 移植纪律

1. **对话内核逻辑等价搬运，先跑通再优化。** 流程切换 / 恢复 / 取消这套任务栈是参考实现里最难的部分，移植时容易手痒重新设计。真要重设计，3–4 天会变成 10 天以上。
2. **消息协议一次定死。** `suggestions` / `cards[]` / 确认卡片的结构在开发早期定完，前后端同时按新协议走。
3. **知识层不要照抄。** 参考实现把一个知识意图下所有 Provider 的结果直接拼接，这是要改掉的行为。

------

# 附录 C. 工作量与实施顺序

## C.1 MVP（14–18 人天）

| 顺序 | 内容 | 人天 |
|---|---|---:|
| 0 | 环境搭建：`docker-compose up` 起中台与 MySQL | 0.5 |
| 1 | **移植对话内核**：流程引擎、命令、状态、规划、澄清、闲聊 | 3–4 |
| 2 | 消息协议（`suggestions` / `cards[]`）+ 后端产出结构化回复 | 1–2 |
| 3 | **RAG 基础版**：入库流水线、分片溯源、阈值检索、未命中兜底（选型已定，见 C.4） | 3–4 |
| 4 | FAQ 检索 | 1 |
| 5 | 商品推荐（含中台商品检索接口） | 2–2.5 |
| 6 | 人工接管（状态标记 + 提示语 + 会话状态查询接口） | 0.5 |
| 7 | 前端：移植对话窗与卡片，Meta 风格视觉重写 | 4–5 |
| | **合计** | **14–18** |

**开发工具（不计功能预算）**：单条测试接口 0.5 人天。演示中不可见，但 RAG 调阈值与切分粒度时省下的调试时间超过成本，建议在第 3 步前先做。

------

## C.2 第二档（11.5–12.5 人天）

| 内容 | 人天 |
|---|---:|
| 下单流程 + 写操作幂等 + 中台创建订单接口 | 3 |
| 接管移交包 + 落库 | 1.5 |
| 商家接管台 | 2–3 |
| 技能指令注入 | 2 |
| 业务信息配置 | 1 |
| 评测用例集 | 2 |

------

## C.3 第三档（10–13 人天）

| 内容 | 人天 | 备注 |
|---|---:|---|
| 业务事件 + 主动补偿 | 4–5 | 演示效果最好，考虑提前 |
| 品牌化专项任务 | 2 | |
| 全链路 trace | 2–3 | |
| 网页知识抓取 | 1–2 | |
| 知识盲区回流 | 1 | |
| 知识路由优先级层 | 1 | 若知识库开始放商品说明书则需提前 |

------

## C.4 RAG 技术选型

> **⚠ C.4.1–C.4.8 记录的是第一版选型（Chroma + DashScope `text-embedding-v3`），已于 2026-08-28 完成并通过全部验收。该选型现已被 C.4.9 的重做方案取代。**
> 这几节保留不删，因为重做的对照基线、被推翻的理由、以及「哪些降级当初是对的」都要靠它们才读得懂。**读现行结论请直接看 C.4.9。**

> 通用选型依据见根目录 `RAG_ref.md`。本节是它在本项目约束下的**收敛结论**：`RAG_ref.md` 面向"可扩展到企业级"，本节面向"MVP 三周内跑通且零新增外部服务"，因此在向量库、Embedding、检索策略三处主动做了降级，并写明各自的升级触发条件。

### C.4.1 选型原则

1. **零新增外部服务**：MVP 不改 `docker-compose.yml`，`docker compose up` 起来的仍只有 MySQL 与业务中台。
2. **与既有栈同源**：Embedding 复用 DashScope 凭据与 LangChain 依赖，不引入第二套 SDK / 第二份 API Key。
3. **可替换藏在接口后**：Embedding、向量库、切分器各自只有一个实现，但都经由 `infrastructure/vector_client.py` 等统一接口调用，替换时不动 `knowledge/` 以上任何一层（对应 5.4）。
4. **先跑通再优化**：混合检索、重排、缓存全部后置。MVP 阶段影响答案质量最大的是切分粒度与阈值，不是排序算法。

------

### C.4.2 分环节结论

| 环节 | MVP 选型 | 规模化替换 | 理由 |
|---|---|---|---|
| 文档解析 | Python 直读 Markdown / TXT / CSV | Apache Tika（PDF / DOCX） | MVP 知识源是自己写的政策文档，无需多格式解析 |
| 切分 | LangChain `RecursiveCharacterTextSplitter`，按 `\n## / \n### / \n\n / 。` 递归 | 标题感知切分器（保留章节路径） | 依赖里已有；递归分隔符已能在标题与段落处断开 |
| Embedding | DashScope `text-embedding-v3`（1024 维） | 自托管 `bge-m3` / `sentence-transformers` | 与 `qwen-plus` 同源，省一套凭据；中文表现好于通用多语模型 |
| 向量库 | Chroma（本地持久化目录） | PGVector → Qdrant / Milvus | 单机进程内可跑，元数据过滤够用，无需起容器 |
| 检索 | 余弦相似度 Top-K + 阈值 + metadata 过滤 | 混合检索（BM25 + 向量）+ 重排 | 见 C.4.5 |
| 生成 | `qwen-plus`（既有） | 高价值场景按需升配 | 不为 RAG 单独换模型 |
| 缓存 | **不做** | Redis 缓存高频问答与 Embedding | MVP 无并发压力，缓存会掩盖阈值调错的问题 |

这套方案零新增外部服务、零新增凭据。**本节即决策结论，MVP 第 3 步可开工。**

------

### C.4.3 向量库候选对比

| 候选 | 形态 | 优 | 劣 | 何时选它 |
|---|---|---|---|---|
| **Chroma** | 进程内 + 本地目录持久化 | 零部署、API 简单、metadata 过滤够用 | 并发与数据量上限低，无副本 | **MVP（本项目选型）** |
| PGVector | MySQL 换 / 加 PostgreSQL 扩展 | 与业务库同栈、事务与备份现成 | 要动 `docker-compose.yml`，需 Postgres | 知识源上万分片、需要与业务数据联表 |
| Qdrant | 独立容器，Rust | 轻量高性能、过滤能力强、上手快 | 多一个服务与运维面 | 需要并发与持久化，但不想上 Milvus |
| Milvus / Weaviate | 集群 | 企业级扩展、治理完善 | 部署与运维成本显著 | 百万级分片、多租户 |
| FAISS | 纯库 | 最快、无服务 | 无 metadata 过滤、无持久化语义、需自管落盘 | 只做离线评测 / 基线对比 |

**迁移触发条件**（满足任一即从 Chroma 升级，不必等到出事）：分片总数 > 5 万；检索 P95 > 300ms；需要多进程并发写索引；需要索引的备份与回滚。

> 抽象边界只需覆盖四个方法：`upsert(chunks)` / `query(vector, top_k, filters)` / `delete(source_id)` / `count()`。控制住这四个签名，换库是半天的活；让 Chroma 的返回结构漏进 `knowledge/handler.py`，就是两天。

------

### C.4.4 Embedding：托管还是自托管

| | 托管（DashScope `text-embedding-v3`） | 自托管（`bge-m3` / MiniLM） |
|---|---|---|
| 质量 | 中文稳定，开箱可用 | 相当，需自行验证 |
| 成本 | 按量计费，知识库量级下可忽略 | 无调用费，但需 GPU 或忍受 CPU 延迟 |
| 隐私 | 商家政策文档出网 | 数据不出机房 |
| 运维 | 无 | 模型下载、版本管理、批量化 |

**本项目选托管**：MVP 的知识源是商家公开政策与商品说明，本就要对消费者展示，无隐私顾虑；省下的一天运维时间直接投给切分与阈值校准更划算。

> 两条硬约束：**Embedding 模型一旦更换必须全量重建索引**（维度与向量空间都变），因此模型名要与索引一起落库；**入库与检索必须用同一个模型**，配置项只留一处，杜绝两边配不一致。

------

### C.4.5 检索策略演进

| 档位 | 策略 | 解决什么 |
|---|---|---|
| **第一档** | 向量 Top-K + 相似度阈值 + metadata 过滤（知识源类型 / 名称） | 基本召回与可溯源；阈值负责"不知道就说不知道" |
| **第二档** | 知识源优先级路由，**命中即止**（对应 3.1.2 附注与 B.6 第 3 条） | 消除多 Provider 结果无差别拼接 |
| **第二档** | 查询改写（多轮对话里把"它多少钱"补全为完整问题） | 多轮指代导致的召回崩塌 |
| **第三档** | 混合检索：BM25 + 向量，RRF 融合 | 专有名词、型号、条款编号这类关键词命中 |
| **第三档** | Cross-encoder 重排：ANN 取 Top-20 → 重排取 Top-5 | 提升 Top-K 内的排序精度 |

> `RAG_ref.md` 建议一开始就上「Top-20 + 重排」。本项目不这么做：知识源规模是几十篇文档，向量召回本身已足够，重排带来的是延迟与一个额外模型。等评测集（第二档）跑出来、确认排序确实是瓶颈时再加。
> 第三档的混合检索与重排未单列在附录 C.3。**这两项已于 2026-08-28 提前到当下实施**，不再按第三档处理——见 C.4.9。当时按 2–3 人天计的估算偏低：单看混合检索 + RRF + 重排三段确是 2–3 人天，但它们依赖的向量库与 Embedding 更换是另外 3.5–5 人天，整体 9.5–14。**「加个重排」从来不是一个独立可加项。**

------

### C.4.6 评估与校准

**MVP 阶段（人工，随开发进行）**：准备 20–30 条真问题，覆盖三类——文档里有明确答案的、答案分散在多段的、知识库里根本没有的。第三类必须全部走兜底，一条编造都不能有。

**第二档（评测用例集，见 C.2）**：

| 层 | 指标 | 目标 |
|---|---|---|
| 检索 | Top-K 命中率、MRR | 命中率 ≥ 90% |
| 生成 | 忠实度（回答未超出分片信息） | 100%，超出即缺陷 |
| 生成 | 兜底正确率（该兜底时兜底） | 100%，编造即缺陷 |
| 系统 | 端到端 P95 延迟 | ≤ 3s |

> 忠实度与兜底正确率是本项目的**红线指标**，不是优化目标——它们对应 5.2 与验收标准 2、3。检索命中率可以慢慢调，编造一次就是产品事故。

**阈值校准方法**：用上述用例跑一遍，记录每条的最高相似度；有答案的那批取最低值、无答案的那批取最高值，阈值落在两者之间。若两个区间重叠，说明切分粒度有问题，先改切分而不是改阈值。

------

### C.4.7 成本、隐私与降级

- **成本**：Embedding 只在入库与每次提问各调一次，知识库量级下成本可忽略；真实成本在 LLM 生成侧，靠 Top-K 与上下文总长上限（见 3.1.2 默认参数）控制，不靠减少检索。
- **隐私**：知识源限于商家对外可见内容。若后续引入内部资料（成本价、供应商合同），需先切到自托管 Embedding，此为**准入前提**而非优化项。
- **降级**：向量库或 Embedding 服务不可用时，直接回"暂时查不了，帮你转人工"，**不得退化为模型自身知识作答**（对应 5.1）。这条降级路径要在第一档就实现，不能等出故障时再补。

------

### C.4.8 落地对照（写代码时看这张表）

| 目录 / 文件 | 承载 |
|---|---|
| `knowledge/ingest/` | 加载 → 切分 → 向量化 → 写索引，附一条命令行入库脚本 |
| `infrastructure/vector_client.py` | C.4.3 那四个方法，Chroma 实现 |
| `infrastructure/llm_client.py` | Embedding 调用与 LLM 调用共用 DashScope 凭据 |
| `knowledge/provider/rag.py` | Top-K + 阈值 + 未命中返回空（而非兜底话术，兜底由 responder 决定） |
| `knowledge/provider/provider.py` | `KnowledgeChunk` 加来源标识与相似度（B.4 已列） |
| `repository/knowledge_repository.py` | 知识源与分片的元数据落库（类型、名称、入库时间、Embedding 模型名） |
| `config/settings.py` | Embedding 模型、向量库路径、分片大小 / 重叠、Top-K、阈值 —— 全部环境变量驱动 |

------

### C.4.9 选型重做：Milvus + BGE-M3 混合检索（2026-08-28 决策，现行）

第一版选型（C.4.1–C.4.8）已完整落地并通过 7.1 全部验收。本节记录**推翻它的决策**、施工步骤与工时。

**决策依据**：对齐 `knowledge_base/atguigu` 的技术栈。该项目与本项目共用同一份 `RAG_ref.md` 作为选型依据，但走的是完整版路线；本项目第一版走的是 MVP 路线。两者不是对错之分，是同一份文档在不同约束下的两个落点。**本次决定收敛到完整版。**

> 需要写明的一点：C.4.5 曾以「知识源只有几十篇文档，向量召回本身已足够，等评测集证明排序是瓶颈时再加」为由拒绝重排与混合检索，而**评测集并未证明排序是瓶颈**——基线实测有答案召回 22/22，Top-K 内没有漏召。因此本次重做**不是被数据推动的，是选型对齐决策**。这条如实记下，以免后来人误以为是性能问题倒逼。

#### C.4.9.1 分环节变更

| 环节 | 第一版（C.4.2） | 重做后 | 性质 |
|---|---|---|---|
| Embedding | DashScope `text-embedding-v3`，托管，1024 维 dense | **BGE-M3 本地**，一次产出 dense + sparse 双向量 | 托管 → 自托管 |
| 向量库 | Chroma，进程内本地目录 | **Milvus standalone**（etcd + minio + milvus 三容器） | 零新增服务 → 新增三个容器 |
| 检索 | 余弦 Top-K + 阈值 + metadata 过滤 | **混合检索**（dense + sparse）+ **RRF 融合**（`weight/(k+rank)`，k=60）+ **rerank** | 单路 → 三段 |
| 查询改写 | 无 | **HyDE**：LLM 先生成假设性答案，再对答案检索 | 新增 |
| 编排 | 普通函数调用 | **LangGraph `StateGraph`**，入库与查询各一张图 | 新增 |
| 切分 | `RecursiveCharacterTextSplitter` | 不变 | 保留 |
| 语料 | 45 片，中文 | 不变 | 保留 |

**rerank 用 DashScope 托管的 `TextReRank`，不需要 GPU。** 这是三段里最便宜的一段——`knowledge_base` 那边的 `reranker_http_utils.py` 只有 50 行。

#### C.4.9.2 本机硬约束（估算的前提）

| 约束 | 实测 | 后果 |
|---|---|---|
| 无 CUDA | Apple M1 Max | `knowledge_base/pyproject.toml` 强制走 `download.pytorch.org/whl/cu128`，**本机装不上**，须改用默认 PyPI 的 arm64 轮子；`use_fp16` 必须关闭 |
| Docker 内存 8GB | MySQL 已占一部分 | Milvus standalone 官方建议 ≥8GB，45 片量级跑得动但需上调 Docker Desktop 上限 |
| 磁盘剩 32GB | BGE-M3 权重 ~2.3GB + torch 全家桶 + Milvus 镜像 ~2GB | 够用，装完约剩 25GB |

#### C.4.9.3 施工步骤与工时

| Phase | 内容 | 人天 |
|---|---|---:|
| **0** | **固定基线**：用 34 条校准集跑一遍现行实现，冻结对照数字 | 0.5 |
| **1** | 基础设施：`docker-compose` 加 Milvus 三件套（独立 project name）；`vector_client.py` 扩接口支持 `AnnSearchRequest` + `WeightedRanker`；BGE-M3 本地化 | 2–3 |
| **2** | 入库侧重建：embedding 换 BGE-M3 产双向量；Milvus collection 建两个向量字段；**全量重建索引** | 1.5–2 |
| **3** | 检索侧三件套：混合检索 + RRF → rerank → HyDE | 2–3 |
| **4** | **重新校准**：阈值卡在哪一层（向量分 / RRF 分 / rerank 分）、Top-K 与 rerank TopK 配比 | 1 |
| **5** | LangGraph 编排：入库与查询改写成 `StateGraph` | 2–3 |
| **6** | 验收：spec-auditor 重跑 7.1 第 1/2/3 条 + 与 Phase 0 基线逐项对照 | 1 |
| — | 保留 Chroma 作为 `VECTOR_BACKEND` 可切换退路（支持 A/B 对照与回滚） | +0.5 |
| | **合计** | **9.5–14** |

**Phase 0 不可跳过。** 没有基线，「换了更先进的技术栈」是一句无法证伪的话。基线已冻结在 `customer-service-backend/knowledge_eval/BASELINE_chroma_dashscope.md`。

**Phase 5 对答案质量零贡献**，是纯架构对齐。若工期紧张，砍它不影响检索效果。

#### C.4.9.4 改造后必须对照的五项

1. **有答案召回** —— 基线 22/22，不得低于
2. **无答案正确兜底** —— 基线 11/12，目标 12/12
3. **「支持货到付款吗」那条误通过** —— 基线里唯一的失败项（top=0.7263，与「支持海外或港澳台配送吗？」表面句式相似）。混合检索的 sparse 分量理论上能区分这两者，**这是本次改造最该兑现的一条**；若仍误通过，说明 RRF 与 rerank 没有解决实际问题
4. **端到端延迟** —— 基线单条检索 0.68–0.85 秒。改造后大概率变差（BGE-M3 走 CPU，且多了 HyDE 一次 LLM 调用与 rerank 一次 API 调用）。**允许变差，但要量出来，不能估**
5. **「未命中不调 LLM」的机制保证** —— 现行实现里未命中、低于阈值、向量库不可用三条路径都在建立 LLM chain 之前 return 常量，编造在机制上不可能发生。**LangGraph 改写不得破坏这一点**，这是验收 7.1 第 3 条与 5.2 红线

#### C.4.9.5 哪些留、哪些扔

| 保留 | 废弃 |
|---|---|
| `knowledge_source/` 语料（45 片不重写） | Chroma 实现（降级为可切换退路） |
| `ingest/loader.py`、`splitter.py` 切分逻辑 | `text-embedding-v3` 调用路径 |
| `KnowledgeChunk` 的溯源字段 | **阈值 0.58 与那 34 条的校准结论** |
| `retrieval_traces` 表 | `vector_client.py` 的四方法接口（dense+sparse 装不进去，须扩） |
| `KnowledgeResponder` 的兜底保证 | |
| `engines/builder.py` 注册、`Provider` 抽象 | |

**最贵的隐性成本是阈值作废。** BGE-M3 的分数分布与 `text-embedding-v3` 不同；加 rerank 之后「分数」的语义再变一次（rerank 分是相关性打分，不是余弦）。34 条要重跑，且阈值该卡在三段中的哪一层需要重新决定。

------

# 附录 D. 前端视觉规范（Meta Business Suite 风格）

参照 Meta 官方业务工具的视觉语言：登录页提供页面氛围（浅色渐变、大标题、胶囊按钮），控制台提供工作区语言（白面板、深色导航选中态、蓝色主按钮）。聊天界面属于工作区，页底沿用渐变。

## D.1 设计 token

| 用途 | 值 |
|---|---|
| 页底 | `linear-gradient(135deg, #f6f2fa, #eef3fc 55%, #e9f1fb)` |
| 面板 / 卡片 | `#ffffff` |
| 次级面底（消息气泡、输入框） | `#f0f2f5` |
| 主色 | `#1877f2` |
| 主色浅底（标签、选中行） | `#e7f0fd` |
| 导航选中态 | `#1c2b33` 深色实心 |
| 边框 | `#dadde1`；胶囊按钮 `#dddfe2` |
| 正文 / 次要文字 / 占位符 | `#050505` / `#65676b` / `#8a8d91` |
| 字体 | 系统字体栈（Meta 的 Optimistic 未公开授权） |

## D.2 形状与动效

| 元素 | 规则 |
|---|---|
| 面板 / 卡片 | 圆角 8–10px，1px 实线边框，无阴影 |
| 主按钮 | 圆角 6px，纯色填充 |
| 次级按钮 / 快捷回复 | 胶囊 `border-radius: 99px`，白底 + 细边框 |
| 消息气泡 | 圆角 16px；用户侧蓝底白字，Agent 侧 `#f0f2f5` 深字 |
| 状态标签 | 胶囊，浅色底 + 同色系深字 |
| 动效 | 仅保留 hover 底色变化与 150ms 过渡，无位移、无缩放、无扫光 |

## D.3 布局

沿用参考实现的三栏结构，左栏改变职责：

| 栏位 | 参考实现 | 本项目 |
|---|---|---|
| 左栏 | 数字人舞台 + sender_id 输入 | **控制台导航**：对话 / 知识库 / 技能 / 接管台 |
| 中栏 | 对话窗 | 对话窗（结构不变，视觉重做） |
| 右栏 | 业务对象列表 | 业务对象列表（结构不变，视觉重做） |

左栏改为导航后，配置层与接管台有了自然入口，不必另开页面。第二、三档的知识库、技能页面直接挂在此处。

## D.4 移除的视觉元素

canvas 粒子系统与连线、双光晕轨道动画、页底渐变动画、全部 `backdrop-filter`、teal / amber 渐变文字、`--shadow-glow-*` 系列、Turn 卡片 hover 扫光、头像旋转、Google Fonts 引入。

## D.5 Turn 卡片

参考实现把每轮对话包在带 `Turn N` 徽章的卡片里，这是调试视角，Meta 风格中没有对应物。

**处置**：保留轮次概念但弱化呈现——去掉外框与徽章底色，改为浅灰小字标记，消息回归左右对齐的气泡。调试需要时可通过开关恢复完整卡片。

------

# 附录 E. 消息协议（一次定死）

> 对应 3.2 的要求与 B.6 第 2 条。前后端按同一份协议走，不接受二次修改。本附录是唯一权威定义。

## E.1 结构

`ChatObject` 不变：`{ id, title, type, attributes }`，`type` 为 `"product"` / `"order"`。

`POST /api/chat` 响应：

```jsonc
{
  "message_id": "...",
  "control_owner": "AGENT",          // 新增。AGENT | PENDING_HUMAN | HUMAN，缺省 AGENT
  "messages": [
    {
      "text": "为你找到 3 款符合预算的",
      "object": null,                 // 保留，兼容旧行为；等价于 cards 只有一项
      "cards": [ /* ChatObject, ... */ ],   // 新增，缺省 []
      "suggestions": ["看看更便宜的", "换个颜色"]  // 新增，缺省 []
    }
  ]
}
```

`GET` 历史接口的响应同样带 `control_owner`。

## E.2 三条规则

1. **一条 bot 消息可以同时有 text、cards、suggestions**。这是与旧行为最大的区别——旧的 `BotMessage` 是 text 与 object 二选一，导致带卡片时快捷回复被丢弃。
2. **`object` 与 `cards` 不并存**。后端要么发 `object`（单个，旧路径），要么发 `cards`（列表，新路径）。前端归一化：`cards?.length ? cards : (object ? [object] : [])`，因此后端尚未改造时前端也能正常工作。
3. **`control_owner` 是会话级而非消息级**，挂在响应顶层。`HUMAN` 时前端禁用输入并显示接管提示（对应 3.3.4 第一档的「界面显示当前控制权归属」）。

## E.3 落地位置

| 端 | 文件 | 改动 |
|---|---|---|
| 后端 | `domain/messages.py` | `BotMessage` 加 `cards: list[FocusedObject]`、`suggestions: list[str]`，**`to_dict` / `from_dict` 两个方法都要改**，否则状态回读时静默丢失 |
| 后端 | `api/schemas.py` | `ChatBotMessage` 加同名字段；`ChatResponse` / `ChatHistoryResponse` 加 `control_owner` |
| 后端 | `api/chat_router.py` | `_build_chat_response` 透传新字段 |
| 前端 | `src/App.vue` | 归一化按 E.2 第 2 条；渲染文本 + 卡片列表 + 快捷回复三段共存 |

> 前端可以先行——按 E.2 第 2 条写的归一化对今天的后端返回完全兼容，后端补齐后无需再改前端。
