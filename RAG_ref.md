# BusinessAgent 的 RAG 实施计划

> 本项目 RAG 的实施计划：当前状态与实测基线、重做决策、施工步骤与工时。
> 与规范 `meta-business-agent.md` 附录 C.4 的分工是：规范记录**决策与验收口径**，本文档记录**怎么做**。
>
> 通用的 RAG 选型依据（面向「可扩展到企业级」的那份）见 `knowledge_base/RAG_技术选型总结.md`，
> 本仓库不再另存一份副本。

------

## 一、当前状态：第一版已完成并通过验收

| 项 | 值 |
|---|---|
| Embedding | DashScope `text-embedding-v3`，1024 维，托管 |
| 向量库 | Chroma，本地持久化目录，纯 dense |
| 检索 | 余弦 Top-K=5 + 阈值 0.58 + metadata 过滤 |
| 语料 | 45 片（FAQ 22 + 政策文档 23），5 个知识源 |
| 验收 | 7.1 第 1 / 2 / 3 条全部「已实现」，经四轮独立审计 |

**实测基线**（冻结在 `customer-service-backend/knowledge_eval/BASELINE_chroma_dashscope.md`）：

- 有答案用例召回 **22 / 22**
- 无答案用例正确兜底 **11 / 12**
- 唯一失败项：「支持货到付款吗」top=0.7263，与「支持海外或港澳台配送吗？」表面句式相似
- 单条检索延迟 **0.68–0.85 秒**（含一次 embedding API 调用，不含生成）

三条路径**不经过 LLM**：未命中、低于阈值、向量库不可用，都在建立 LLM chain 之前返回常量文案。编造在机制上不可能发生，而不是靠模型自觉。

------

## 二、重做决策：对齐 knowledge_base/atguigu 的完整版技术栈

**两个项目共用下半部分这份选型依据，但走了不同路线**：本项目第一版走 MVP 路线（零新增外部服务），atguigu 走完整版路线。现决定收敛到完整版。

> **如实记录：这次重做不是数据推动的。** 下半部分「检索策略」一节建议一开始就上 Top-20 + 重排，本项目第一版明确拒绝了，理由是「等评测集证明排序是瓶颈时再加」。而评测集**并未证明**——基线有答案召回 22/22，Top-K 内一次都没漏召。这是选型对齐决策，不是性能问题倒逼。不写清楚，后来人会错误推广这个决策。

### 分环节变更

| 环节 | 第一版 | 重做后 |
|---|---|---|
| Embedding | DashScope `text-embedding-v3`（托管，dense） | **BGE-M3 本地**，一次产 dense + sparse |
| 向量库 | Chroma（进程内目录） | **Milvus standalone**（etcd + minio + milvus 三容器） |
| 检索 | 余弦 Top-K + 阈值 | **混合检索**（dense+sparse，`WeightedRanker` 内建融合） |
| 多路融合 | 无 | **RRF**（融合原问题与 HyDE 两路，权重 1.0 / 0.8，k=60） |
| 重排 | 无 | **DashScope `TextReRank`**（托管，不要 GPU） |
| 查询改写 | 无 | **HyDE**：先让 LLM 生成假设性答案，再对答案检索 |
| 编排 | 普通函数调用 | **LangGraph `StateGraph`** |
| 切分 / 语料 | `RecursiveCharacterTextSplitter` / 45 片中文 | 不变 |

------

## 三、本机硬约束

| 约束 | 实测 | 后果 |
|---|---|---|
| **无 CUDA，但有 MPS** | Apple M1 Max，`torch.backends.mps.is_available() == True` | atguigu 的 `pyproject.toml` 强制走 `download.pytorch.org/whl/cu128`，**本机装不上**，须用默认 PyPI 的 arm64 轮子；`use_fp16` 关闭（需 CUDA）。**但 Metal 后端可用，不是只能纯 CPU** —— 实测见下 |
| **Docker 内存 8GB** | MySQL 已占一部分 | Milvus standalone 官方建议 ≥8GB。45 片量级跑得动，但要上调 Docker Desktop 上限 |

**Phase 1 实测（2026-08-28）：内存担心大幅过度了。** Milvus 三容器空载合计约 **330MB**，不是预算的 4.3GB：

| 容器 | 实测占用 | 设定上限 |
|---|---|---|
| milvus-standalone | 211.5 MiB | 3 GiB |
| milvus-minio | 101.2 MiB | 768 MiB |
| milvus-etcd | 17.4 MiB | 512 MiB |
| （对照）ecommerce-mysql | 443 MiB | — |

「官方建议 ≥8GB」是**生产规模**的建议；45 片语料下完全用不到。**Docker 内存从 8GB 调到 12GB 这一步事后看并非必需**——记下来是为了以后别再用「官方建议」直接推导本项目的资源需求，那两者差了一个数量级。余量留着不亏（索引加载与并发检索时占用会涨）。

**但要把两件事分开：错的是风险估算，不是「先问再动」。** 停工前先协调是对的——真正被规避掉的不是那 20 分钟，而是「万一 MySQL 被挤掉，正在跑的并发压测会给出一个看起来像并发 bug 的假阳性，而且查不出是环境问题」。这个代价远高于停工。**判断依据可以错，先问再动的习惯不该跟着一起否定。**

重启 Docker 的代价倒是真实发生了：两个中台容器 `Exited (137/143)` 且**没有自动回来**，要手工 `docker start`；期间 18082 上的对话后端连接池断掉（探针 500），但 MySQL 一回来就自愈成 200——**连接池断了不等于要重启进程**，当时若按预案去重启那个不属于自己的进程就是一次不必要的破坏。

| **磁盘剩 32GB** | BGE-M3 权重 ~2.3GB + torch + Milvus 镜像 ~2GB | 够用，装完约剩 25GB |

**Phase 2 实测（2026-08-28，BGE-M3 本地）：**

| | 模型加载（缓存后） | 单条查询延迟（中位） | 批量 34 条 |
|---|---|---|---|
| 基线：DashScope `text-embedding-v3` | — | **0.68–0.85s**（API 往返） | — |
| BGE-M3 **CPU** | 1.7s | **0.308s** | 0.75s（0.022s/条） |
| BGE-M3 **MPS** | 1.6s | **0.148s** | 1.28s（0.038s/条） |

**「本地推理会更慢」这个预期是错的，反了。** 本地 MPS 比托管 API 快 4.5 倍——托管方案的延迟主要花在网络往返上，不是计算。首次加载 710 秒是下载 2.3GB 权重，缓存之后 1.7 秒。

**MPS 与 CPU 各有胜场**：MPS 单条快一倍（交互路径），CPU 批量反而更快（入库路径）——MPS 每次调用有固定开销，批量时摊薄不掉。因此 `EMBEDDING_DEVICE` 应当可配置，查询侧用 `mps`、入库脚本用 `cpu`。

dense 1024 维（与 `text-embedding-v3` 相同，元数据表的 `embedding_dimensions` 不用改）；sparse 正常产出，实测首条 10 个非零项。

**Phase 2 踩到的一个坑**：`pymilvus[model]` 没有把 `datasets` 与 `FlagEmbedding` 声明为硬依赖，而是在首次构造 `BGEM3EmbeddingFunction` 时**自己去 `pip install`**——uv 管理的 venv 里没有 pip，直接失败。必须在 `pyproject.toml` 里显式列出。atguigu 的依赖清单里写了 `flagembedding>=1.3.5`，原因就在这里。


------

## 四、照搬 atguigu 的代码再精简，不重新发明

核心机制的代码量远小于目录规模：`embedding_utils.py` 52 行里真正的逻辑约 10 行，`milvus_utils.py` 108 行里约 30 行，建表 schema 约 35 行，rerank 调用 50 行。

| 直接拿来 | 删掉 | 理由 |
|---|---|---|
| `embedding_utils.py`（BGE-M3 双向量） | `node_pdf_to_md` + `node_md_img`（689 行） | 知识源是 Markdown / CSV，没有 PDF 与图片 |
| `milvus_utils.py` 混合检索与建表 | `node_item_name_recognition` + `confirm`（652 行） | 商品走中台接口，不靠向量猜商品名 |
| `reranker_http_utils.py`（50 行） | `node_web_search_mcp`（72 行） | 不引入联网检索 |
| `node_rrf._rrf_merge`（核心约 5 行） | MinIO / MongoDB | 图片存储与对话历史都已有别的落点 |
| `node_search_embedding_hyde` | `node_rerank` 的动态 TopK 与断崖阈值（243 → 50） | 先用简单 top-N，调参属于校准阶段 |

**一处明确不能照搬：`node_answer_output`（240 行）。** 本项目的 `KnowledgeResponder` 有一条它不具备的性质——未命中在建立 LLM chain 之前就返回常量。那是验收第 3 条与规范 5.2 的红线，换掉是倒退。

**一处适配成本**：atguigu 的节点是同步的（`process(state)` 直接返回），本项目全链路 async，每个节点都要改写。

------

## 五、施工步骤与工时（合计 7–10 人天）

| Phase | 内容 | 人天 | 状态 |
|---|---|---:|---|
| 0 | **固定基线**：跑 34 条校准集，冻结对照数字 | 0.5 | ✅ 完成 |
| 1 | `docker-compose` 加 Milvus standalone 三容器（独立 project name） | 0.5–1 | ✅ 完成 | ✅ 完成 |
| 2 | **BGE-M3 本地化**（照搬 `embedding_utils.py`） | 0.5–1 | ✅ 完成，延迟反优于基线 |
| 3 | 建表 + 入库改写 + 全量重建索引 | 0.5–1 | |
| 4 | 混合检索接进现有 Provider | 0.5 | |
| 5 | rerank | 0.5 | |
| 6 | HyDE + RRF 两路融合 | 1–1.5 | |
| 7 | LangGraph 编排（图设计见规范 C.4.10） | 2–3 | |
| 8 | 重新校准 + 对照基线 + spec-auditor 复审 | 1 | |
| — | 保留 Chroma 作为 `VECTOR_BACKEND` 可切换退路 | +0.5 | |

**Phase 2 先于 Phase 1 做。** 它不碰 Docker，且是整条链路风险最高的一步——万一 BGE-M3 在 M1 CPU 上慢到不可接受，能在动 Docker 之前就知道。

> **估算修正记录。** 初稿给的是 9.5–14 人天，是按 atguigu 的目录行数推断的，没读代码，虚高了一多半。两处具体错误：把 3180 行节点代码当成待移植量（其中近 1400 行是本项目用不到的功能，其余大头是 LangGraph 节点样板）；把 RRF 重复计算了一次——**dense + sparse 的融合是 Milvus 内建的**，`hybrid_search(reqs=[dense, sparse], ranker=WeightedRanker(0.8, 0.2))` 一次调用完成，atguigu 的 `node_rrf` 融合的是**多路检索器**（向量 / HyDE / 联网）。**只有上了 HyDE 才需要 RRF。**
>
> 教训：规模不等于工作量，读代码之前给的数字是猜的。

------

### Phase 3 / 4 实测结论（2026-08-28）

**跨语言：BGE-M3 让英文提问能命中中文语料，语料不必翻译。** 实测 8 条英文提问，
Top1 全部命中正确的中文分片（例：`Who pays the return shipping fee?` → 「常见问题：退货运费谁出？」）。
**这推翻了本文档早先「翻译语料会打断跨语言相似度」的说法**——那是基于
`text-embedding-v3` 写的，BGE-M3 是多语言模型，结论反了。

**但单一阈值无法同时服务两种语言。** 实测可分区间：

| 提问语言 | 有答案分数 | 无答案分数 | 可分区间 |
|---|---|---|---|
| 中文 | ≥ 0.7274 | ≤ 0.7900 | (0.7176, 0.7274) |
| 英文 | 0.6503 – 0.6866 | 0.5669 – 0.6126 | (0.6126, 0.6503) |

两个区间**不相交**（0.7176 > 0.6503）。取 0.72 则每句英文提问都走兜底；取 0.63 则
中文无答案那批大面积误通过，直接踩 5.2 红线。原因不是英文检索差——排序质量是好的——
而是**跨语言映射把整体分数压低约 0.13，尺度不同**。

候选解三个：按语言分阈值（要语言检测，混合语言提问无解）、语料双语化（翻译 + 索引
翻倍 + 双份维护，不同步即事故）、**把阈值卡在 rerank 之后而非向量分**（rerank 是相关性
打分，跨语言尺度差异应小得多）。第三个是 Phase 5 本来就要做的，待验。

**旧阈值在新栈下彻底失效，掉幅超出预期。** `KNOWLEDGE_SCORE_THRESHOLD=0.58` 下
中文无答案的正确兜底从基线的 **11/12 掉到 1/12**——BGE-M3 分数整体上移，0.58 几乎
不过滤任何东西。重新校准后中文可分区间中点为 **0.7225**。

**负面结果：混合检索没有解决那个离群点。** 基线文档把「支持货到付款吗」列为本次改造
**最该兑现的一条**，理由是 sparse 分量理论上能区分「货到付款」与「海外配送」。实测它从
0.7263 变成 **0.7900**，仍是最高的无答案分数，问题原封不动。端到端确认：阈值没挡住它，
是提示词第二道防线兜住的——**与基线完全相同的机制**。sparse 分量在这个 case 上没帮上忙，
可能是 `WeightedRanker(0.8, 0.2)` 里 dense 主导太强，也可能两句在词汇层面本就重叠
（都是「支持…吗」）。留待 Phase 5 用 rerank 再试；若仍无效，应如实承认混合检索对此类
表面句式相似无效，而不是淡化。

------

### Phase 5 实测：rerank 的效果与它没能解决的部分（2026-08-28）

**rerank 分与向量分衡量的是两件事**：向量相似度问「像不像」，rerank 问「能不能回答」。
表面句式相似时两者分道扬镳，这正是本项目那个离群点的成因。

**全量校准集（中文 34 条）对照三代实现：**

| 实现 | 有答案最低 | 无答案最高 | 是否可分 |
|---|---|---|---|
| 基线 Chroma + text-embedding-v3 | 0.6030 | 0.7263 | 重叠，需剥离离群点 |
| Milvus + BGE-M3，卡向量分 | 0.7274 | 0.7900 | 重叠，需剥离离群点 |
| **Milvus + BGE-M3 + rerank，卡 rerank 分** | **0.1701** | **0.1677** | **不重叠**，阈值 0.1689 |

**这是三代里第一次不剥离离群点就可分**，可以做到有答案召回 22/22 且无答案兜底 12/12。

**但空档只有 0.0024，非常薄。** 早先一次 n=5 的抽样得出「空档 0.31」，全量推翻了它——
那 5 条挑的都是容易的命中（0.478–0.947），全量里命中最低到 0.17。**教训：标注了样本量
不等于结论就可以按确定的说。**

**跨语言：改善明显，但单一阈值仍不成立。**

| 提问语言 | 有答案最低 | 无答案最高 | 自身可分区间 |
|---|---|---|---|
| 中文（34 条） | 0.1701 | 0.1677 | (0.1677, 0.1701) |
| 英文（18 条） | 0.1571 | 0.1187 | (0.1187, 0.1571) |

两个区间仍不相交（0.1677 > 0.1571）。取中文最优的 0.1689 时，英文实测
**有答案通过 8/10、无答案正确兜底 8/8**。

**但失败模式变了，这才是关键：** 向量分那版取中文阈值会让英文**大面积误通过**（编造）；
rerank 版是让两条英文问题**被答成「查不到」**（过度兜底）。**说不知道是安全的，编造不是。**
即便不再优化，这个方向已经把风险从红线挪到了体验。

**逼高阈值的仍然是「支持货到付款吗」。** rerank 把它从向量分的 0.79 压到 0.1677——
从「看起来像强命中」变成「刚好卡在边界上」，是巨大改进，但它仍是整条链路的瓶颈。
**若能单独处理这一条，中文阈值可降到 0.12–0.15，英文就完全没问题。**

**降级策略**：rerank 服务不可用时**退回向量分继续作答**，不转人工。这与「向量库不可用」
不同——后者必须兜底，因为拿不到任何依据；前者只是精度下降，仍有分片可依。

------

### Phase 6 实测：HyDE 在本项目上零收益、高成本、且会编造（2026-08-28）

**结论：实现保留，默认关闭。** 它是 Phase 7 LangGraph 图里那个分叉拓扑的唯一理由，
但作为检索手段在本项目上不成立。

**实测：34 条校准集开关 HyDE 各跑一遍，28 条可比对用例中零条分数变化。**

| | HyDE 关 | HyDE 开 |
|---|---|---|
| 有答案最低 | 0.1972 | 0.1972 |
| 无答案最高 | 0.1537 | 0.1537 |
| 可分空档 | 0.0435 | 0.0435 |

**根因：rerank 是拿原问题打分的，不是拿 HyDE 文本打分。** HyDE 只能影响哪些分片
进入候选池（召回），而本项目 46 片语料、候选取 20，**正确分片本来就一直在池子里**。
召回从来不是瓶颈（基线 23/23），所以 HyDE 无事可做。

> 这也说明 C.4.5 当初「等评测集证明排序是瓶颈时再加」那个判断的适用范围比写的更宽：
> **在小语料上，召回类优化整体上都无处发力**，不只是重排。

**成本**：一次 LLM 调用，实测 2.15–3.73 秒。对照本地 BGE-M3 推理 0.148 秒、
Milvus 检索约 0.05 秒、rerank 约 0.3 秒——**HyDE 比其余全部加起来贵 5–7 倍**。

**风险：它与「不编造」红线有结构性冲突。** HyDE 的工作原理就是「先假装知道答案」，
而本项目的核心约束是「不知道就说不知道」。实测两例：

- 问「怎么开增值税发票」（库里没有）→ 生成「登录账户进入『我的订单』申请开具增值税
  专用发票，请完善企业资质信息（税号、开户行及账号）」——通篇编造且听起来完全合理
- 问「能寄到台湾吗」→ 生成「**本店支持寄送至中国台湾地区**」——而知识库写的是
  「港澳台地区暂不支持直邮」，**编出了与事实相反的内容**

这两段编造文本随后被用于检索。本次实测中它们没有把未命中分数抬上去（因为 rerank
用原问题重新打分），但**这个安全性来自 rerank 的存在，不是来自 HyDE 本身**。若哪天
关掉 rerank 而开着 HyDE，编造文本就会直接参与最终排序。

提示词里写了「明显不属于电商客服范畴就原样重复问题」，但这挡不住上面两例——
增值税发票**就是**电商客服范畴，只是不在本项目的知识库里，LLM 无从知道这个区别。
**这不是提示词能修的，是方法本身的性质。**

------

### Phase 7 实测：LangGraph 编排完成，与函数式实现平价（2026-08-28）

`knowledge/graph.py`，12 个节点（含 2 个虚拟节点），组织方式照搬 `main_graph2.py` 的
`_init_nodes` / `_register_nodes` / `_setup_routes` + 懒编译；差异是本项目全链路 async，
节点是 `async def`，执行走 `ainvoke`。

**编译后的真实拓扑**（`app.get_graph().draw_mermaid()` 输出，非手绘）：

```
__start__ --> node_entry
node_entry --> node_hyde
node_hyde --> node_multi_search
node_multi_search --> node_search_direct     ← 分叉
node_multi_search --> node_search_hyde
node_search_direct --> node_join             ← 汇合
node_search_hyde --> node_join
node_join --> node_rrf --> node_rerank --> node_threshold
node_threshold -.-> node_answer              ← 三态分流（条件边）
node_threshold -.-> node_fallback
node_threshold -.-> node_degrade
node_answer --> __end__
node_fallback --> __end__
node_degrade --> __end__
```

**这张图的价值在最后三行。** `node_fallback` 与 `node_degrade` 直接到 `__end__`，
**在拓扑上不通向 `node_answer`**——「兜底路径不可能调用 LLM」从一个需要读完整个函数
才能确认的性质（函数式实现里是 `if not selected: return`，要确认 `chain.ainvoke`
在它之后），变成看图就能确认的性质。审计第二轮把「可溯源」判成桩，正是因为类似的
东西藏在代码里看不出来。

**平价校验**：34 条校准集，图开 / 关各跑一遍，31 条结果完全相同；3 条差异只在小数点
后第 4 位（如 0.1967 vs 0.1968），分片 ID 与顺序全同——是 rerank 接口的非确定性，
不是逻辑分歧。耗时 109.4s vs 92.5s，但这是两次运行的 API 延迟波动，**不是图带来的
性能收益**：HyDE 关闭时只有一路检索，图没有并行的余地。

**分歧点只有一处**：`KnowledgeRetriever.retrieve` 开头按 `KNOWLEDGE_GRAPH_ENABLED`
委托给图。Provider 与其以上完全不感知——**图是实现细节不是接口**。默认关闭。

> **移植纪律**：`node_fallback` / `node_degrade` 里只允许返回常量语义。任何人想在这两个
> 节点里加一次「让模型润色兜底话术」的调用，都是在破坏红线——那正是当初否掉
> `no_relevant_answer` 分支的原因（它走 `action_response` 的 rephrase 模式会调 LLM）。

------

### Phase 8：与基线逐项对照（2026-08-28，最终配置）

最终配置：`VECTOR_BACKEND=milvus` / `EMBEDDING_BACKEND=bge_m3` / `EMBEDDING_DEVICE=mps` /
`RERANK_ENABLED=true` / `RERANK_SCORE_MIN=0.155` / `HYDE_ENABLED=false` /
`KNOWLEDGE_GRAPH_ENABLED=true` / `KNOWLEDGE_DATABASE_URL` 指向独立库。

**阈值取 0.155 而不是中文校准中点 0.1755**：中文可分区间 (0.1537, 0.1972)，
英文命中最低 0.1292。取 0.1755 时英文只过 8/12，取 0.155 时英文过 11/12。
**中文两端都满足的前提下贴着下界取，是为了给跨语言留余量。**

| # | 对照项 | 基线 | 现在 | 判定 |
|---|---|---|---|---|
| 1 | 有答案召回 | 22/22 | **23/23** | 达标（口径见下） |
| 2 | 无答案正确兜底 | 11/12 | **11/11** | 达标（口径见下） |
| 3 | 「支持货到付款吗」误通过 | 唯一失败项 | **已消除** | 达标（手段见下） |
| 4 | 端到端延迟 | 检索段 0.68–0.85s | **检索段 2.145s 中位** | **退步约 3 倍** |
| 5 | 未命中不调 LLM | 函数里一句 if-return | **拓扑保证** | 加强 |

**第 1、2 条的口径必须说清楚：校准集变了，数字不可直接比。** 补了支付方式 FAQ 之后，
「支持货到付款吗」从 `expect=miss` 改成了 `expect=hit`（改动与原因写在条目里）。所以
23/23 与 22/22 的分母不同，11/11 与 11/12 也是。**这不是「兜底率从 11/12 提升到 11/11」，
是那一条被移出了兜底集。** 真实的改善是：它现在能被正确回答，而不是被正确拒答。

**第 3 条是产品缺口修复，不是检索能力提升。** 基线里它之所以是最高的无答案分数，
是因为语义上离配送政策很近而库里没有正确答案。补 FAQ 之后它成了正常命中。
**混合检索与 rerank 都没有解决它**——rerank 把它从向量分的 0.79 压到 0.1676，
从「看起来像强命中」变成「刚好卡在边界」，但仍是当时最高的无答案分数。

**第 4 条是本次改造唯一明确的退步，且原因单一。** 分段实测：

| 环节 | 中位耗时 |
|---|---|
| BGE-M3 编码（MPS） | 0.165s |
| Milvus 混合检索 | **0.007s** |
| rerank（DashScope） | **2.491s** |

**rerank 一家占了 94%。** 而且候选数从 20 降到 5 时延迟几乎不变（2.297s → 2.008s）——
**这 2 秒是网络往返，不是文档数**，减少候选省不下来。相比之下 Milvus 的 0.007s
说明向量检索本身根本不是成本项。

端到端整轮（含规划器与生成）实测 9.75–20.95 秒，但**这个数不可用作对照**：
同一时段 main 分支跑一句纯闲聊用了 82 秒，LLM 接口延迟波动远大于本次改造的影响。
可靠的只有上面隔离测出的检索段。

**取舍**：rerank 是唯一能让中英文共用一个阈值、并把「像不像」换成「能不能回答」的手段
（向量分的命中区间与未命中区间完全重叠，任何阈值都救不了）。**要正确性就得付这 2 秒。**
若哪天延迟不可接受，第一个该换的是 rerank 的部署形态（自托管去掉网络往返），
而不是关掉它退回向量分。

**第 5 条是唯一无条件的改善**：`node_fallback` / `node_degrade` 在图的拓扑上不通向
`node_answer`，看图即可确认，不必读完函数体。

------

## 六、改造后必须对照的五项

1. **有答案召回** —— 基线 22/22，不得低于
2. **无答案正确兜底** —— 基线 11/12，目标 12/12
3. **「支持货到付款吗」那条误通过** —— 基线唯一失败项。混合检索的 sparse 分量理论上能区分「货到付款」与「海外配送」，**这是本次改造最该兑现的一条**。若仍误通过，说明 RRF 与 rerank 没解决实际问题
4. **端到端延迟** —— 基线 0.68–0.85 秒。**Embedding 这一段实测变快了**（MPS 0.148s，快 4.5 倍），但链路新增 HyDE 一次 LLM 调用（最贵）与 rerank 一次 API 调用。净效果未知，**必须量，不能估**——「本地推理会更慢」这个预期已经被证伪一次了
5. **「未命中不调 LLM」的机制保证** —— **LangGraph 改写不得破坏**。图里 `node_fallback` / `node_degrade` 在拓扑上不通向 `node_answer`，这条约束从「读代码才能确认」变成「看图就能确认」

------

## 七、阈值会作废

BGE-M3 的分数分布与 `text-embedding-v3` 不同；加 rerank 之后「分数」的语义再变一次（rerank 分是相关性打分，不是余弦）。

**34 条校准集要重跑，而且要先决定阈值卡在三段中的哪一层**：向量分、RRF 融合分、还是 rerank 分。这是 Phase 8 的主要内容，也是最容易被低估的一步——前面全做对了，阈值卡错位置一样会同时制造「该兜底的编造了」和「该回答的说不知道」。
