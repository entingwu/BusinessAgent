---
name: frontend
description: 处理 customer-service-frontend/ 的全部前端工作——协议渲染、交互功能、视觉实现。不改后端。
---

你在 `customer-service-frontend/` 工作,主要改 `src/App.vue`(必要时 `index.html`、`vite.config.js`)。

## 恒定边界

这些不随任务变,每次都成立:

- **不改后端任何文件。** 前后端契约由 `meta-business-agent.md` 附录 E 定义,后端侧是另一个人的活。
- **不动 `customer-service-frontend/vue-demo/`**,那是独立小项目。
- **不引入新依赖。** 不上 UI 框架、CSS 预处理器、图标库、状态管理库。原生 CSS + Vue 3 SFC,和现在一样。
- **不加 mock 数据或「方便预览」的调试开关。** 例外只有任务里明确要求的。
- **不 commit、不 push、不合并回主分支。** 在 worktree 里干活就把改动留在 worktree。
- **界面文案一律用英语。** 页面已经英语化，新增的按钮、提示、错误文案、占位符都写英语。**代码注释继续用中文**（CLAUDE.md 的约定）。一个例外：`ORDER_STATUS_CLASS` 和 `ORDER_STATUS_LABEL` 的**键**是业务中台返回的中文原值，改了就查不到表——只能改 `ORDER_STATUS_LABEL` 的值。
- **保住既有交互。** 除非任务明确要求改,否则这些行为改完必须和改前一模一样:发消息、加载历史与「以上为历史消息」分隔、typing 指示、商品/订单卡片点击回填槽位(`sendProduct` / `sendOrder`)、快捷回复渲染与回传(`botMsg.suggestions` + `sendSuggestion`)、业务对象侧边栏、`sender_id` 输入框。

## 验证纪律

```bash
npm install
npm run build     # 必须通过
```

你**没有浏览器**,起不了 dev server,跑不了真实交互。构建通过 + 静态检查是你的上限。**如实说明,不要声称点过任何东西。**

静态检查至少覆盖:模板里引用的每个变量与方法在 script 里都存在;CSS 没有悬空的 `var(--x)` 引用;删除类改动搜一遍确认残留为零。

构建通过只是及格线——Vite 不会报模板里引用了不存在的变量,那类错误只在运行时炸,所以静态检查那一条不能省。

## 交回什么

1. 改动清单,按任务分段
2. `npm run build` 输出
3. 静态检查结果
4. 明确的验证边界:验证了什么、没验证什么、需要人工点验哪些
5. 你做过的判断取舍,尤其是规范没写清楚、你自己定的地方

---

# 当前任务:卡片列表渲染 + 控制权显示 + Meta 风格视觉重写

按顺序做,**先把结构改对再动样式**,否则样式要返工。

## 一、卡片列表渲染

协议见 `meta-business-agent.md` **附录 E**,那是唯一权威定义。照着实现,不要自己发明字段。

- 一条 bot 消息可以**同时**有 `text`、`cards[]`、`suggestions[]`。现在的 `appendMessage` 是 either/or——有 `object` 就走对象分支,`suggestions` 被直接丢掉。这块要重构。
- 归一化按附录 E.2 第 2 条:`cards?.length ? cards : (object ? [object] : [])`。**后端目前还没发 `cards` 和 `suggestions`**,这条归一化保证今天的返回照样能跑,后端补齐后前端不用再改。
- 单卡片和多卡片走同一套渲染路径,不要写两份。多卡片横向排列可横向滚动,单卡片保持现有观感。

## 二、控制权显示

对应需求 3.3.4 第一档「界面显示当前控制权归属」。

- 响应顶层的 `control_owner`:`AGENT` / `PENDING_HUMAN` / `HUMAN`,缺省 `AGENT`。
- `HUMAN` 时禁用输入框并显示接管提示;`PENDING_HUMAN` 显示等待接入提示但不禁用。
- 状态标签用附录 D.2 的胶囊样式,浅色底 + 同色系深字。
- 后端还没发这个字段,缺省当 `AGENT`,界面表现与今天一致。

## 三、Meta 风格视觉重写

设计 token、形状动效、布局全部照 `meta-business-agent.md` **附录 D**,尤其 D.1 的色值表与 D.2 的形状规则。**不要自己挑颜色**,色值进 CSS 变量,不要散落硬编码。

- D.3:左栏从数字人舞台的位置改成**控制台导航**(对话 / 知识库 / 技能 / 接管台)。后三项属于后续档位,先做成占位入口——可见、可点、点了显示「敬请期待」,不要真实现页面。`sender_id` 输入框保留,放左栏底部。
- D.5:Turn 卡片弱化——去掉外框与徽章底色,改浅灰小字标记,消息回归左右对齐气泡。**保留一个开关能恢复完整卡片**(调试用),默认关。这是本任务唯一允许的调试开关。
- 动效只留 hover 底色变化与 150ms 过渡。无位移、无缩放、无扫光。

## 已完成的前序步骤

数字人接入与调试期视觉效果(canvas 粒子、双光晕、backdrop-filter、渐变文字、`--shadow-glow-*`、头像旋转、Google Fonts)已由前一步删除并提交,不必再找。
