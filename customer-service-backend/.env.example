# 复制为 .env 并填入真实值。.env 已被 .gitignore 排除，切勿提交。

# LLM（示例为阿里云百炼 DashScope 的 OpenAI 兼容接口）
LLM_MODEL=qwen-plus
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=sk-你的密钥

# 电商业务服务（本地 docker-compose 起的 ecommerce-service-backend）
COMMERCE_API_BASE_URL=http://127.0.0.1:18081

# 数据库
DATABASE_URL=mysql+aiomysql://用户名:密码@127.0.0.1:13306/数据库名?charset=utf8mb4

# 服务监听
APP_HOST=0.0.0.0
APP_PORT=18082

# 应用日志级别（规范 5.3 可观测性）。有默认值 INFO，可以不配。
# 注意这只管 business_agent 这一棵；检索日志由下面的 KNOWLEDGE_LOG_LEVEL 单独控制，
# 两者互不影响——检索日志量大，需要能单独关掉
LOG_LEVEL=INFO

# 商家自定义的转人工关键词，逗号分隔（规范 3.3.4「命中配置关键词」）。
# 留空即不启用这条触发；「转人工」「人工客服」这类内置词不受此项影响
HANDOFF_KEYWORDS=

# Knowledge base / RAG (selection rationale: meta-business-agent.md appendix C.4)
# Embedding reuses LLM_API_KEY / LLM_BASE_URL above — no second credential needed.
# Backend: dashscope (hosted, dense only) | bge_m3 (local, dense + sparse).
# Hybrid retrieval needs sparse vectors, so the Milvus path requires bge_m3.
EMBEDDING_BACKEND=dashscope
# Inference device, bge_m3 only: cpu | mps | cuda.
# Measured on M1 Max — mps is 2x faster per query (0.148s vs 0.308s) but slower
# in batch (0.038s vs 0.022s per item): MPS has a fixed per-call overhead that
# batching cannot amortise. Use mps for queries, cpu for the ingest CLI.
EMBEDDING_DEVICE=cpu
# dashscope backend: text-embedding-v3   |   bge_m3 backend: BAAI/bge-m3
EMBEDDING_MODEL=text-embedding-v3
EMBEDDING_DIMENSIONS=1024
EMBEDDING_BATCH_SIZE=10

# Chroma persistence directory (relative paths resolve against customer-service-backend/)
# Vector store backend: chroma (dense only, fallback) | milvus (dense + sparse hybrid)
VECTOR_BACKEND=chroma
# milvus backend only
MILVUS_URI=http://127.0.0.1:19530
VECTOR_STORE_DIR=knowledge_store/chroma
VECTOR_COLLECTION_NAME=business_knowledge

# Knowledge source documents
KNOWLEDGE_SOURCE_DIR=knowledge_source

# Retrieval parameters. Token counts are estimates — see knowledge/ingest/tokens.py
KNOWLEDGE_CHUNK_SIZE=600
KNOWLEDGE_CHUNK_OVERLAP=100
KNOWLEDGE_TOP_K=5
# Calibrated against knowledge_eval/calibration_set.jsonl, NOT the spec's 0.35.
# text-embedding-v3 cosine scores run high enough that 0.35 filters almost nothing.
# Changing the embedding model OR the chunking invalidates this value — recalibrate:
#   uv run python -m business_agent.knowledge.ingest calibrate
# Vector-score gate. It fires **only** when RERANK_ENABLED=false, or as the fallback when the
# rerank service is down mid-request. While rerank is up, RERANK_SCORE_MIN is the gate that
# actually runs and this value is inert.
#
# The two are on different scales and must never be copied into one another: rerank relevance
# runs ~0.0-0.9 with hits clustered low, vector cosine runs ~0.5-0.8. Putting a rerank-scale
# number here silently disarms the degraded path — everything clears it, and "the fallback cannot
# fabricate" stops being a property of the topology and becomes a matter of prompt compliance.
# That mistake was made and caught here on 2026-08-28; node_threshold in knowledge/graph.py
# carries the mirror-image warning.
#
# 0.75 was derived against the English corpus with RERANK_ENABLED=false:
# answerable 26/29 recalled, unanswerable 8/8 correctly rejected. It is deliberately strict —
# this path only runs during a rerank outage, where refusing to answer beats guessing.
# Loosening it to 0.61 would recall 29/29 but reject only 4/8.
#
# Earlier values, kept because they show how far this moves: 0.58 (text-embedding-v3, Chinese
# corpus) and 0.7225 (BGE-M3, Chinese corpus). Both were voided by englishifying the corpus.
#
# **The Chroma + text-embedding-v3 chain has not been re-calibrated since the corpus changed.**
# After `bash scripts/enable_milvus_rag.sh --revert`, run `ingest --force` then `calibrate` and
# use what it prints. Note calibrate reports against whichever scoring is active, so derive this
# key's value with RERANK_ENABLED=false.
KNOWLEDGE_SCORE_THRESHOLD=0.75
# Same scale, same purpose, but calibrated on Chinese questions. The corpus is English and BGE-M3
# is multilingual, so a Chinese question is a cross-lingual retrieval and scores about 0.10-0.13
# lower. The two bands interleave — the top English miss scores 0.7485, above every Chinese hit —
# so one scalar cannot gate both languages, and which one applies is decided by the script of the
# question (business_agent/knowledge/thresholds.py). Re-derive with:
#   RERANK_ENABLED=false uv run python -m business_agent.knowledge.ingest calibrate \
#     --file knowledge_eval/calibration_set_zh.jsonl
KNOWLEDGE_SCORE_THRESHOLD_CJK=0.625
KNOWLEDGE_CONTEXT_MAX_TOKENS=2000

# Retrieval traceability: log level, and whether to write the retrieval_traces table.
# The server's root logger sits at WARNING; the knowledge package attaches its own
# handler to the business_agent.knowledge logger and this key sets its level.
# Set it to WARNING to silence retrieval logging.
KNOWLEDGE_LOG_LEVEL=INFO
KNOWLEDGE_TRACE_ENABLED=true

# Reranking. When enabled the threshold moves from the vector score to the
# rerank score — they mean different things: vector similarity is "how alike",
# rerank is "can this answer the question". Measured on this corpus the vector
# hit/miss ranges overlap completely while the rerank ranges are separated by
# a 0.31 gap, and the rerank scale holds across languages.
RERANK_ENABLED=false
RERANK_MODEL=gte-rerank-v2
RERANK_CANDIDATES=20
# Calibrated on the English corpus (2026-08-28). Ranges: answerable >= 0.1916,
# unanswerable <= 0.1550 once one outlier is set aside — "What do I need to open a
# shop on your platform" scores 0.2770 against business_info because it is adjacent
# to merchant information without being answered by it. That one is left to the
# prompt's second line of defence, exactly as its Chinese predecessor was.
# Re-derive with `ingest calibrate` after ANY corpus edit; the previous value 0.155
# was calibrated on the Chinese corpus and survived the translation unchanged, which
# left the gate sitting 0.0000x above the second-highest miss.
RERANK_SCORE_MIN=0.173

# HyDE: generate a hypothetical answer with the LLM, retrieve with it too, fuse by RRF.
# This adds an LLM call — measured at 2.0-2.7s, more than the rest of the retrieval
# chain combined. Only worth it if recall is actually the bottleneck.
HYDE_ENABLED=false
HYDE_WEIGHT=0.8

# Orchestrate retrieval with LangGraph. Same results as the function path; the
# difference is that "fallback never calls the LLM" becomes a topology property
# (node_fallback / node_degrade do not route to node_answer) instead of an
# if-return buried mid-function.
KNOWLEDGE_GRAPH_ENABLED=false

# Knowledge metadata connection. Empty = use DATABASE_URL (main's behaviour).
# A branch that changes the embedding model or vector store MUST point this at its own
# database: the vector index is a local gitignored directory while the metadata lives in
# the shared MySQL, so sharing it makes the "vector_chunks == metadata_chunks" check fail
# for everyone else — for reasons unrelated to their own environment.
KNOWLEDGE_DATABASE_URL=
